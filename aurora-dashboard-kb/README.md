# Aurora Dashboard — База знаний

Структурированная база знаний по проекту **aurora-dashboard** (`projects/SAP/aurora-dashboard`).
Рассчитана на двух читателей: Кирилл (планирование, справочник, освежение контекста) и Claude (контекст для сессий планирования и разработки).

> Закреплена на коммите `b7576dbd` (origin/main). Последнее обновление: 12.08.2026.
> При заметных изменениях кодовой базы обновляйте затронутый файл — файлы независимы друг от друга.

## Содержание

| Файл | На какие вопросы отвечает |
| --- | --- |
| [01-overview.md](./01-overview.md) | Что это за проект, стек технологий, структура монорепо, ключевые факты и подводные камни |
| [02-architecture.md](./02-architecture.md) | Паттерн BFF, tRPC, роутинг, авторизация и скоупинг токенов, permissions/policy, SSE, отмена запросов |
| [03-packages.md](./03-packages.md) | Каждый пакет workspace: назначение, точки входа, API, сборка |
| [04-development-workflow.md](./04-development-workflow.md) | Локальная настройка, команды, соглашения по коммитам и PR, CI/CD, релизы, тестирование, агенты и скиллы Claude |
| [05-domain-map.md](./05-domain-map.md) | Карта фич и сервисов: какой роутер, роут и design-doc относится к каждому домену |

Сами файлы 01–05 написаны по-английски — в языке кодовой базы, коммитов и upstream-документации.

## Как использовать базу с Claude

- **В сессиях Cowork / Claude Code по этому репозиторию:** укажите эту папку как контекст или попросите Claude прочитать нужный файл перед планированием (например: «прочитай 02-architecture.md и 05-domain-map.md, потом планируй фичу»). `CLAUDE.md` в корне репозитория уже содержит указатель на эту базу.
- **Планирование новой BFF-фичи:** обычно достаточно 02 (архитектурные паттерны) + 05 (где живут похожие фичи); design-docs в `packages/aurora/docs/` дают глубину по конкретным фичам.
- **Обновление базы:** в сессии с подключённой папкой SAP попросите «обнови базу знаний» — агент выполнит процедуру из раздела ниже. В Claude Code для этого есть скилл `update-kb`.

## Как обновлять базу (инструкция для агента)

Выполняй шаги по порядку. Не сдвигай закреплённый коммит, пока не обновлены затронутые файлы.

1. **Определи диапазон.** Возьми закреплённый коммит `<PIN>` из шапки этого README. Выполни `git fetch origin` (если есть сеть), затем `git log --oneline <PIN>..origin/main`. Пустой список → база актуальна, сообщи об этом и остановись.
2. **Отфильтруй шум.** Коммиты `chore(version): update versions with Changesets` и правки lock-файлов не требуют анализа — из них бери только новые версии пакетов.
3. **Разбери существенные коммиты.** Для каждого: `git show --stat <sha>`, при необходимости — дифф значимых файлов. Проверяй утверждения по фактическому коду, а не по тексту коммит-сообщения: сообщение может приукрашивать или устареть.
4. **Определи затронутые файлы базы.** Стек, структура монорепо, версии, подводные камни → `01-overview.md`. Архитектура, auth, permissions, серверные/клиентские паттерны → `02-architecture.md`. API и сборка пакетов → `03-packages.md`. Команды, CI/CD, конвенции, релизы, тесты → `04-development-workflow.md`. Новые роутеры, роуты, design-docs, карта фич → `05-domain-map.md`.
5. **Обнови затронутые файлы.** Сохраняй язык и стиль каждого файла (01–05 — английский, README — русский). Устаревшие утверждения исправляй, а не дописывай рядом.
6. **Обнови README.** Сдвинь закреплённый коммит на новый HEAD `origin/main`, обнови дату, добавь запись в журнал обновлений сверху списка (в самом низу файла) в формате: `дата · <старый pin> → <новый pin> (N коммитов, PR #x–#y): суть изменений; смена версий`. Если существенных изменений не было — всё равно сдвинь pin и напиши «без существенных изменений». Журнал не сокращай — сохраняй все записи целиком.
7. **Отчитайся.** Кратко расскажи пользователю, что приехало в main и какие файлы базы обновлены.

## Ключевые факты

- **Upstream:** https://github.com/cobaltcore-dev/aurora-dashboard (SAP / cobaltcore-dev, Apache-2.0)
- **Назначение:** современный пользовательский дашборд для OpenStack (преемник легаси-дашборда Elektra), BFF-архитектура
- **Стек:** React 19 + TanStack Router · Fastify + tRPC + Zod · Tailwind v4 · Lingui i18n · pnpm + Turborepo · Vitest + Playwright
- **Публикуемый пакет:** `@cobaltcore-dev/aurora` (библиотека server + client); `apps/dashboard` — тонкое референсное приложение-потребитель
- **Dev-сервер:** `pnpm dev` → http://localhost:4005 (нужен Keystone `IDENTITY_ENDPOINT` в `apps/dashboard/.env`)

## Журнал обновлений

- **12.08.2026 · `1fe65a1b` → `b7576dbd`** (5 коммитов, PR #1092, #1144, #1153, #1157, #1158): восстановлены `PERMISSION_KEY_PATTERN.md`/`PERMISSION_ROUTER_IMPLEMENTATION.md`, случайно удалённые PR #1146 (см. запись от 10.08.2026 ниже) — PR #1158 вернул оба файла дословно, в одном коммите с ним же пришёл фикс CVE-2026-59870 в `js-yaml` (pnpm overrides запинены на патченные `3.x ≥3.15.1`/`4.x ≥4.3.1`). SSRF-фикс пагинации Glance-образов (#1144): новый общий хелпер `packages/aurora/src/server/helpers/urlValidation.ts` (`isAbsoluteUrl`/`validateRelativeUrl`, зачищает управляющие символы `U+0000–U+0020` перед проверкой на `http(s):`/`//`) навешан на курсоры `first`/`next` в `imageRouter`, и Swift-валидация имени аккаунта (`Storage/types/swift.ts`, добавлена в фиксе SSRF от 10.08.2026) переведена на тот же хелпер вместо собственной инлайновой проверки. Path traversal в OpenStack-роутах (#1153): новый `packages/signal-openstack/src/pathHelpers.ts` (`encodeOpenstackPathSegment`/`encodeOpenstackPath`/`validateAndEncodeResourceId`, отклоняет `.`/`..`/`./`, слэши, `?`/`#`) проведён в `flavorHelpers`/`flavorRouter`, `floatingIpRouter`, `securityGroupRouter`, `projectRouter`, `pcaRouter`; `SignalOpenstackError` экспортирован как класс (не тип) и замаплен в `wrapError` на `BAD_REQUEST` вместо `INTERNAL_SERVER_ERROR`; **тот же класс уязвимости остаётся непокрытым** в `imageRouter.memberId`, `rbacPolicyRouter.policyId`, `securityGroupRuleRouter.ruleId` (зафиксировано в `prs/1153-…md`, новыми коммитами не тронуто). Смержен PR #1092 — управление CORS для Ceph/S3-бакетов (переработан из модалки в отдельную вкладку «CORS Rules»); контент уже был полностью описан в `prs/1092-…md` (4 раунда ревью), новых деталей для 01–05 нет, только смена статуса. PR #1157 — тривиальный фикс ссылок на пакет в changeset'ах (только build, без продуктового кода), в базу не роутится. Версии пакетов не менялись — aurora `0.23.1`, dashboard `1.2.9` (много pending changesets ждут релизного PR). Статусы PR-отчётов #1153 (open → смержен 12.08.2026) и #1092 (черновик → смержен 12.08.2026) обновлены в `prs/README.md` и в самих отчётах.
- **12.08.2026 · без сдвига пина (не диапазон коммитов, а локальная реорганизация)**: контент из `aurora-dashboard/docs/agent-patterns.md` (файл существовал только на ветке `kirylDev`, не в `origin/main` и туда не планировался) перенесён в базу после проверки каждого утверждения по актуальному коду, а сам файл и ссылка на него из `CLAUDE.md` удалены из репозитория — субагенты (`dev-planner` и ревьюеры), которые читают `CLAUDE.md` + эту базу, раньше файл не видели вовсе, ссылка из `CLAUDE.md` была на него единственной. Перенесено то, чего не было ни в одном файле 01–05: механизм дедупликации rescope-токена (`sessionRescopes` в `server/context.ts`) и уточнение, что status-aware `parseOrThrow`/`ErrorHandler` в Network помечен в самом коде как `WORK_IN_PROGRESS`/прототип (в отличие от широко принятого `withErrorHandling`) → `02-architecture.md`; стратегия выбора tRPC-линка (`trpcClient.ts`, таблица `splitLink`), изоляция query-кэша по проекту (`queryKeyHashFn`), разница `useDomainId`/`useProjectId` (сессия vs URL) → туда же; паттерны тестирования — фабрика `mockContext.ts`, глобальные моки `vitest.setup.ts`, условная обёртка `I18nProvider`, стаб `global.fetch` в signal-openstack, немоканный `policy-engine` integration-тест, `debugTrace.ts` для разбора отказа в правах → `04-development-workflow.md`. Не перенесено — то, что дублировало уже более полное и живое описание в этих же файлах: список процедурных билдеров, route conventions, permission-key convention, описание policy-engine.

- **10.08.2026 · `46256e98` → `1fe65a1b`** (7 коммитов, PR #1148, #1121, #1146, #1149, #1150, #1152, #1154): security-фикс SSRF в Swift — параметр `account` теперь валидируется Zod-схемой на формат `AUTH_<identifier>` (алфанумерика/`_`/`-`), отклоняет абсолютные HTTP(S)-URL и `../`-traversal (#1148, CWE-918/200). Смержен PR #1121 — bulk delete объектов Ceph (S3): `objectRouter.deleteBulk` чанкует `DeleteObjects` по 1000 ключей, схема запрещает удаление «папок» (trailing `/`) и ограничивает массив 1–10000 ключей, при системной ошибке бросает, при частичном отказе возвращает смешанный результат — **известный баг остаётся**: `RestoreVersionModal`/`DeleteVersionModal` игнорируют `errorCount` и показывают успех даже при отказе S3 (см. `prs/1121-…md`, план-фикс `plans/2026-08-03-…md`); статусы PR #1121 и связанного плана в `prs/README.md` и `plans/README.md` были ошибочно «не смержен»/«not implemented» — исправлено этим обновлением. CI: добавлена вторая инфраструктура PR-превью — `pr-preview.yml` (self-hosted runner + Traefik + `docker-compose.yml`, порт 3000) параллельно уже существующему `build-push-aurora-pr-preview.yaml` (GHCR + ArgoCD) — обе сейчас в `origin/main` одновременно (#1146), деблокирована через `continue-on-error` + отдельный `preview-check` job для branch protection (#1152). Node поднят до `24.15.0` в `.nvmrc` (#1149, там же upgrade jsdom/undici). Security-патчи зависимостей: `@fastify/static` → `10.1.2` + override `fast-uri` ≥`3.1.5` (SSRF/desync CVE, #1150), `js-yaml` 3.x/4.x-ветки запинены на патченные версии (CVE-2026-59870, #1154) — оба через `pnpm-workspace.yaml` overrides. Версии пакетов не менялись — aurora `0.23.1`, dashboard `1.2.9`. **⚠️ Найдено при верификации (не по тексту коммита):** PR #1146 ("add PR preview infrastructure") в одном из сквошенных коммитов ("chore: remove documentation files") удалил из `origin/main` `PERMISSION_KEY_PATTERN.md` и `PERMISSION_ROUTER_IMPLEMENTATION.md` — оба файла отсутствуют в дереве `origin/main` (проверено `git cat-file -e`), хотя на них ссылаются `04-development-workflow.md`, `05-domain-map.md` и `01-overview.md`. Файлы всё ещё есть локально на ветке `kirylDev` (она ответвилась до этого удаления), но выглядит как непреднамеренная потеря апстрим-документации — стоит завести issue/PR на восстановление в `cobaltcore-dev/aurora-dashboard`. Сеть до GitHub из этой сессии была недоступна (proxy 403) — диапазон коммитов и содержимое диффов проверены по локально закэшированному `origin/main`.

- **06.08.2026 · `f367c07b` → `46256e98`** (6 коммитов, PR #1120, #1123, #1124, #1129, #1131, #1132): в Ceph (S3) добавлены пре-подписанные ссылки — `objects.generatePresignedUrl` подписывает `GetObjectCommand` через `getSignedUrl` из `@aws-sdk/s3-request-presigner` учётными данными EC2 текущего запроса, без похода в Ceph и без ключа (в отличие от Swift temp URLs), возвращает `{ url, expiresAt }` (абсолютный unix-таймстемп, чтобы UI показывал реальный срок), `expiresIn` ограничен в Zod-схеме константой `S3_PRESIGN_MAX_EXPIRY_SECONDS` (604800 = 7 дней, предел SigV4); UI — `GeneratePresignedUrlModal` с пресетами 1 час / 24 часа / 7 дней и кастомной длительностью (#1120). Таблицы объектного хранилища теперь меряют **обе** границы: нижняя привязана к верхнему краю футера, который ищется по собственному классу приложения `.app-page-footer` (обёртка вокруг слота `pageFooter`, добавлена в `__root.tsx`, чтобы не зависеть от вёрстки Juno) и отдельно наблюдается `ResizeObserver`'ом — кастомный футер любой высоты больше не перекрывает последние строки и не оставляет зазор; фолбэк без футера — низ вьюпорта минус 52px, пол снижен с 200px до 150px, параметр `bottomGap` у `useAvailableViewportHeight`/`useVirtualizedTableBody` удалён (#1123). Состояние списка floating IPs (фильтры, поиск, сортировка) переехало в URL: `validateSearch` c пофайловым `safeParse`-фолбэком (невалидный параметр обнуляет только себя, а не весь тулбар), sort/filter/мгновенный поиск делают `push` в историю, дебаунс-ввод — `replace` (#1129). Тосты Glance-образов мигрировали с легаси `<Toast>` на общий `NotificationManager`: билдеры возвращают `{ message, ...options }`, severity выбирает вызывающий код (`toast.success`/`error`/…), локальный `setToastData` убран, обработчики ошибок ужесточены опциональными цепочками; `NotificationText` осиротел и остался мёртвым кодом (#1132). Плюс мелочи: тултип лимитов Swift-контейнера снова показывается по ховеру (`TooltipTrigger` без `asChild`, `Icon` завёрнут в `span`) и у `createMockContext` появился явный возвращаемый тип (#1124); из корневого `package.json` убрана зависимость `pnpm: ^11.16.0`, конфликтовавшая с `packageManager` и ронявшая Docker-сборку, **pnpm поднят с `11.18.0` до `11.20.0`**, из `.github/actions/setup-pnpm` снова убран хардкод версии (#1131). Версии пакетов не менялись — aurora `0.23.1`, dashboard `1.2.9`; changeset'ы (1 minor за #1120 + патчи) ждут релизного PR. Статусы PR-отчётов #1120, #1123, #1132 переведены в «смержен».

- **04.08.2026 · `cf2e0ad` → `f367c07b`** (1 коммит, PR #1122): без существенных изменений в коде продукта — dev-зависимости обновлены патч/минорными версиями (`@changesets/cli`, `commitizen`, `eslint`, `prettier`, `prettier-plugin-tailwindcss`, `turbo`), **pnpm поднят с `10.33.0` до `11.18.0`** (`packageManager` в package.json); `pnpm-workspace.yaml` — supply-chain overrides (`minimumReleaseAgeExclude`/`overrides`) перенесены из `package.json.pnpm.overrides` и расширены (`@fastify/static`, `esbuild`, `fast-uri`, `find-my-way`, `postcss`, `lodash`, `js-yaml`, `shell-quote`, `brace-expansion`, `tmp`), плюс `allowBuilds` для `@swc/core`/`esbuild`; CI (`ci-checks.yaml`, `release.yaml`) больше не пинит `PNPM_VERSION` явно — версия берётся из `packageManager`; починен флейки-тест breadcrumb в `project-detail.spec.ts` (текст сломался из-за смены формата «Domain/Project»); остальные диффы — только prettier-переформатирование (без изменения логики). aurora `0.23.1` не менялась.

- **31.07.2026 · `fa875c7` → `cf2e0ad`** (3 коммита, PR #1094, #1099, #1111): floating IPs — расширены поля поиска (`floating_ip_address`, `fixed_ip_address`, `floating_network_id`), шапка списка переведена на `DataGridToolbar` (SortInput/FiltersInput/SelectedFilters + debounced SearchInput), `placeholderData` убирает блокирующий спиннер при рефетче (только на первой загрузке), кнопка «Allocate» теперь проверяется через `network.canUser` вместо хардкода прав, убран неиспользуемый bulk-selection UI (#1099); общий компонент `TwoColumnDescriptionList` вынесен из floating-ip-специфичной папки в `client/components/` и переиспользован в detail-view Flavor/Image/FloatingIp/SecurityGroup вместо ручных `DescriptionTerm`/`DescriptionDefinition` пар (#1111); aurora `0.23.0 → 0.23.1`, dashboard `1.2.8 → 1.2.9` (патч-релиз, содержательных изменений в CHANGELOG кроме уже учтённого fa875c7 нет).

- **25.07.2026 · `a7ad5d1` → `fa875c7`** (8 коммитов, PR #952, #1073, #1085–#1090, #1093): вынос переиспользуемых хуков `useAvailableViewportHeight`/`useVirtualizedTableBody` — таблицы Ceph/Swift теперь высчитывают высоту по факту оставшегося вьюпорта (с полом в 200px), а не по `max-height`, что убирает цикл ре-измерений виртуализатора (#1090); загрузка объектов в Ceph (S3) бакеты — файловый пикер/drag-and-drop, прогресс, отмена, через отдельную `cephUploadProcedure` (октет-стрим не мержится с `project_id` инпутом обычной scoped-процедуры, поэтому метаданные идут заголовками `x-upload-*`) (#1086); React-зависимые пакеты (`@lingui/*`, `@tanstack/react-*`, `@trpc/react-query` и др.) перенесены из `dependencies` в `peerDependencies` — иначе консьюмер получал дублирующийся React и падающие хуки; заодно у `policy-engine`/`signal-openstack` убрано поле `exports`, и dev-скрипт дашборда теперь watch'ит исходники воркспейс-пакетов напрямую (#1085, новый design doc `0014_dependency_classification.md`); мердж security-group фичи (data grid, permission-gated actions через `useSecurityGroupPermissions`, синхронизация toolbar с URL) (#952); фикс показа tRPC-сообщений об ошибках на root/project error boundary вместо общего фолбэка (#1073); монохром-шрифт убран из нетехнических списков UI (#1088); aurora `0.22.0 → 0.23.0`, dashboard `1.2.7 → 1.2.8`.
- **20.07.2026 · `28cc3ff` → `a7ad5d1`** (1 коммит, PR #1062): загрузка объектов Ceph S3 (скачивание/превью) вынесена в отдельный Web Worker — стриминг, base64-декод и сборка Blob больше не блокируют главный поток; владение трансфером живёт в module-scope сторе вне React (`objectDownloadStore.ts`, `useSyncExternalStore`), переживает размонтирование таблицы при навигации по папкам; общий toast на все параллельные загрузки; отмена — кооперативная (abort воркера + graceful terminate через 5с). Воркер инлайнится (`?worker&inline`), поэтому требует `worker-src 'self' blob:` в CSP — сервер Aurora теперь выставляет эту директиву сам в продакшене (`server.ts`); библиотечная сборка (`vite.config.mjs`) подставляет `process.env.NODE_ENV` через `define`, иначе воркер падает без `process`. Версия пакета не менялась (changeset minor ожидает релиза).
- **16.07.2026 · `566338f` → `28cc3ff`** (3 коммита, PR #1074–#1080): восстановлен слот `slots.login` для кастомной формы логина — рендерится на лендинге (`/`) вместо дефолтной `LoginForm` для неавторизованных (#1079; слот был молча потерян при рефакторинге auth #1072); aurora `0.20.3 → 0.22.0` (0.21.0 — релиз изменений #1072/#1075, 0.22.0 — login-слот).
- **15.07.2026 · `a2f31f1` → `566338f`** (6 коммитов, PR #1067–#1075): из BFF удалён глобальный rate limit (`@fastify/rate-limit` выпилен, #1075); рефакторинг авторизации — tRPC-вызовы auth централизованы в `AuthProvider`, авто-логаут по истечении сессии с возвратом на прежний URL, новая `LoginForm` на Juno-компонентах (#1072, minor-релиз aurora); оптимистичный рендеринг страницы проектов через Suspense/`useSuspenseQuery` (#1067); фикс CreateBucketModal при пустом списке бакетов (#1069); aurora `0.20.1 → 0.20.3`.
- **15.07.2026 · первая версия** на коммите `a2f31f1`.
