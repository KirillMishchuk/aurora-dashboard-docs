# 02 — Architecture

## Big picture

```
Browser (React 19, TanStack Router)
   │  tRPC over HTTP (default prefix /polaris-bff)
   ▼
BFF: Fastify + tRPC  (packages/aurora/src/server)
   │  signal-openstack (undici)          │ AWS SDK v3 (S3)
   ▼                                     ▼
OpenStack APIs (Keystone, Nova, Neutron, Glance, Swift…)   Ceph RGW (S3)
```

- The client is stateless and does no direct OpenStack calls; all contracts are Zod-validated and typed end-to-end via tRPC.
- The BFF owns session cookies, token scoping/rescoping, policy evaluation, retries and API stitching.

## Server side (BFF)

### Entry point

`createServer(config)` in `packages/aurora/src/server` builds and returns a `FastifyInstance` (before `.listen()`), so consumers can register extra plugins/routes. Key config: `identityEndpoint` (required, Keystone v3), `policyDir` (required), `bffEndpoint` (default `/polaris-bff`), `viteRoot`, `defaultEndpointInterface`, `proxyUrl` (dev only), `cephRegion`, `imageMetadataExcludedProperties`, `cookieName`/`cookieDomain`/`crossDomainCookie`/`insecureCookies`, `routers` (custom tRPC routers).

Security middleware in the stack: `@fastify/helmet`, `@fastify/csrf-protection` (client caches CSRF token, see commit `4518889`), cookie-based session (`dashboard-session-auth` by default). Note: the global rate limit (`@fastify/rate-limit`, 200 req/min) was removed in #1075 — the BFF currently has no built-in rate limiting. **Plugin registration order matters** (#1203, merged 25.08.2026): the CSRF plugin must be `server.register`ed *before* any route it should protect. `server.ts` also registers an optional direct (non-tRPC) HTTP endpoint, `/upload-image-direct`, as a fallback image-upload path — it was originally registered ahead of `AuroraFastifyCsrfProtection`, letting a same-site attacker bypass CSRF protection on that cookie-authenticated route; fixed by moving the CSRF `server.register` call up in `server.ts`, before route registration.

### Domain module convention

Each OpenStack domain lives in a PascalCase folder under `src/server/`:

```
Compute/
  routers/    # tRPC routers (one per resource: serverRouter, flavorRouter, keypairRouter, imageRouter, permissionRouter…)
  types/      # Zod schemas + inferred types
  helpers/    # pure helpers
  *.test.ts   # colocated vitest tests
```

Domains: `Authentication`, `Project`, `Compute`, `Network`, `Storage` (swift + ceph subtrees), plus `policies/` (permission router factory) and `aurora-fastify-plugins/`. There was a built-in `Services` domain (PCA/Clavis: `pcaRouter`, `types/pca.ts`) — removed wholesale in #1189 (merged 24.08.2026, ~7.5k lines deleted: server router/types, all client PCA routes/components/tests) in favor of consumers registering their own project-scoped services via `additionalProjectServices` (see "AuroraApp component" below). `packages/aurora/docs/0011_clavis.md` still describes the old built-in implementation and is now stale — the PR didn't remove or update it.

### Procedure builders (exported from `@cobaltcore-dev/aurora/server`)

`auroraRouter`, `publicProcedure`, `protectedProcedure`, `projectScopedProcedure`, `domainScopedProcedure`, `projectScopedInputSchema`, `domainScopedInputSchema`.

Rules:
- Custom/consumer routers **must** be created with `auroraRouter` (same initTRPC instance) or context types (`ctx.openstack`, `ctx.validateSession`, `ctx.rescopeSession`) break at runtime.
- Typical pattern: `protectedProcedure.input(zodSchema).query/mutation` → `ctx.rescopeSession({ projectId })` → `session.service("compute").get("os-keypairs")` → parse with Zod (`safeParse`) → return typed data.

### Error handling

Two styles coexist. **Legacy** (some handlers, e.g. parts of `Compute/routers/serverRouter.ts`): try/catch the whole handler body, `schema.safeParse` with manual field-by-field logging, and on any failure — missing service, parse failure, thrown error — silently `return undefined`, pushing failure handling onto the client. **Generic wrapper, broadly adopted**: `withErrorHandling(operation, operationName)` (`server/helpers/errorHandling.ts`) runs an async operation and normalizes any non-`TRPCError` into a `TRPCError` via `wrapError`, rethrowing an already-thrown `TRPCError` as-is — used across Compute (images), Network (floating IP, RBAC policy, security group + rules), and Storage (Swift) routers (previously also Services/PCA, removed #1189 — see "Domains" above). Since #1153, `wrapError` special-cases `SignalOpenstackError` (validation failures thrown by signal-openstack's path helpers, see "Path & URL validation" below) into `BAD_REQUEST` instead of the generic `INTERNAL_SERVER_ERROR` fallback — client-side input mistakes now surface as 400s, not 500s. **A third, tRPC-middleware-level layer was added in #1209** (issue #1128, merged 27.08.2026): `openstackErrorMiddleware` (`trpc.ts`) wraps every procedure and converts a thrown `SignalOpenstackApiError` (now exported as a value, not just a type, so middleware/callers can `instanceof`-check it) into the matching `TRPCError` code — `UNAUTHORIZED`/`NOT_FOUND`/`FORBIDDEN` based on the upstream OpenStack HTTP status — independent of whether the individual router also uses `withErrorHandling`. Written against the tRPC v11 middleware contract (`next()` returns a `MiddlewareResult`, doesn't throw — the middleware checks `result.ok` rather than try/catch). **Network adds a further, narrower layer on top**: `parseOrThrow(schema, data, context)` (`Network/helpers/index.ts`) throws a `PARSE_ERROR` `TRPCError` instead of swallowing a Zod parse failure, and a per-resource `ErrorHandler(resourceName)` factory (`Network/helpers/errorHandling.ts`) maps HTTP status codes (400/401/403/404/409/412) to typed `TRPCError`s with resource-specific messages — currently wired up for floating IPs (previously also `pcaRouter.ts`, removed #1189). The file's own comment marks this status-mapping layer `WORK_IN_PROGRESS`/prototype ("only used for list procedures in Port and Network helpers... goal is to extend this... in the future") — treat `withErrorHandling` as the actually-established convention to follow, and `parseOrThrow`/`ErrorHandler` as a newer pattern still confined to Network, not yet a repo-wide standard.

### Path & URL validation (SSRF / path traversal)

Two small, independent guards, both added in security-fix PRs and both worth reusing rather than re-inventing per-router:

- **Path-segment validation** (`packages/signal-openstack/src/pathHelpers.ts`, #1153): `encodeOpenstackPathSegment(segment, label)` rejects `.`/`..`/`./`, slashes, and `?`/`#`, then `encodeURIComponent`s the segment; throws a `SignalOpenstackError` (exported as a class, not just a type, since #1153, so callers can `instanceof`-check it) on rejection. `encodeOpenstackPath(...segments)` joins several through it; `validateAndEncodeResourceId(id, resourceType)` is the resource-ID-flavored wrapper (doesn't require UUID shape, just rejects path syntax) — use this for any OpenStack resource ID interpolated into a request path. Wired into `flavorHelpers`/`flavorRouter`, `floatingIpRouter`, `securityGroupRouter`, `projectRouter` (previously also `pcaRouter`, removed #1189). **Known gap** (per `prs/1153-…md`): `imageRouter.memberId`, `rbacPolicyRouter.policyId`, and `securityGroupRuleRouter.ruleId` still interpolate their ID param unvalidated — same class of bug, not yet covered.
- **Absolute-URL / SSRF validation** (`packages/aurora/src/server/helpers/urlValidation.ts`, #1144): `isAbsoluteUrl(url)` strips leading control characters (`U+0000`–`U+0020`) then tests for `http(s):` or scheme-relative `//`; `validateRelativeUrl(url, paramName)` throws `BAD_REQUEST` if so. Used by `imageRouter` to reject absolute pagination URLs (`first`/`next` cursors) coming from the client — an attacker-supplied absolute URL would otherwise be handed straight to signal-openstack's fetch, i.e. SSRF via pagination. `Storage/types/swift.ts`'s Swift-account Zod schema (`SWIFT_ACCOUNT_PATTERN`, from the 10.08.2026 SSRF fix — see this KB's update log) now delegates its absolute-URL check to `isAbsoluteUrl` too, instead of its own inline `startsWith` check, so both call sites share one hardening pass (e.g. the control-character stripping) instead of drifting apart.

### Secret redaction & TempURL hardening (Swift storage)

- **Swift metadata secret redaction** (`packages/aurora/src/server/helpers/secretHelpers.ts`, #1173): `parseAccountInfo`/`parseContainerInfo` (`Storage/helpers/swiftHelpers.ts`) used to return the raw `x-account-meta-temp-url-key(-2)`, `x-container-meta-temp-url-key(-2)` and `x-container-sync-key` header values as `tempUrlKey`/`tempUrlKey2`/`syncKey` fields — anyone who could read account/container info could read the secrets needed to mint Swift TempURLs. `redactAccountSecrets`/`redactContainerSecrets` now return presence-only booleans (`hasTempUrlKey`, `hasSyncKey`); `accountInfoSchema`/`containerInfoSchema` (`Storage/types/swift.ts`) were changed to match, a breaking change for any consumer reading the old fields (changeset: `major`). The same file exports a generic `isSecretHeader`/`REDACTED_HEADERS` check (also covering `x-auth-token`, `x-subject-token`, `authorization`), currently only exercised by the two Swift helpers above.
- **TempURL generation hardening** (`generateTempUrlInputSchema`, `Storage/types/swift.ts`, #1176): `method` is now `z.enum(["GET"])` (was `GET|PUT|POST|DELETE`) — TempURLs are read-only by default; `expiresIn` is clamped to `MAX_TEMP_URL_LIFETIME` (604800s / 7 days — raised during review from an initial 3600s cap to support sharing files with colleagues) with a floor of 60s and a `DEFAULT_TEMP_URL_LIFETIME` of 86400s (24h). Changeset marked `minor` despite being a breaking change — same mismatch already flagged for #1173, see `prs/1176-…md`.

### Session token exposure & ownership-confusion fixes (merged 24.08–26.08.2026)

- **Raw bearer token no longer served to the client** (`Authentication/routers/sessionRouter.ts`, #1185): the `getAuthToken` procedure — which returned `token.authToken` straight to browser JS, defeating the HTTP-only session cookie's XSS protection — was deleted outright (no remaining consumers, checked at HEAD). `getCurrentUserSession` no longer returns the raw `tokenData` either; it now returns an explicit allowlist (`user`, `roles`, `project`, `domain`, `catalog`, optional `expires_at`/`issued_at`), so a future field added to `tokenData` server-side doesn't leak to the client by default the way it used to.
- **EC2 credential deletion — IDOR fix** (`Storage/routers/ceph/ec2CredentialRouter.ts`, #1182): `delete` used to call the identity service straight away with only `credentialId` from the input, no check that the credential belonged to the caller — any authenticated user could delete any other user's EC2 credential by guessing/enumerating IDs. Now fetches the credential first, compares both `user_id` and `project_id` against the caller's session token, and returns `NOT_FOUND` (not `FORBIDDEN`) on a mismatch — deliberately indistinguishable from "doesn't exist," so the endpoint can't be used to enumerate other users' credential IDs. A missing credential (already deleted) also returns success — the mutation is idempotent.
- **Floating IP creation — ownership confusion fix** (`Network/routers/floatingIpRouter.ts`, #1206, merged 26.08.2026): `create` used to send the client-supplied `tenant_id`/`project_id` straight to Neutron in the request body — a caller could allocate a floating IP attributed to a `project_id` other than the one their session was actually scoped to. Both fields are now derived server-side from the rescoped session token (`ctx.openstack.getToken().tokenData.project.id`), ignoring whatever the client sent; `FloatingIpCreateRequestSchema`'s `project_id` is still required as **input** (consumed by `projectScopedProcedure` for rescoping) but is no longer trusted as the value forwarded to Neutron. Same PR also documented the cross-subdomain trust-boundary implications of `createServer`'s `cookieDomain` config option inline.
- **Upload-progress cross-project leakage + stale-session reuse fix** (`Compute/routers/imageRouter.ts` + `client/store/AuthProvider.tsx`, #1179, merged 25.08.2026): Glance image-upload progress tracking (`uploadProgress` map, `uploadProgressEmitter`, `watchUploadProgress` subscription) was keyed only by `uploadId` — any authenticated caller who guessed/observed another user's `uploadId` could subscribe to their upload progress across project boundaries. Keys are now `${projectId}:${uploadId}` derived from the session token, and `watchUploadProgress` rejects any client-supplied `uploadId` that already contains a colon (blocks a double-scoping attack that would forge another project's scoped key). Separately, `AuthProvider`'s `logout()` and auto-expiry `handleExpiry()` now `await trpcClient.auth.terminateUserSession.mutate()` (with visible error state on failure) **before** clearing local session state and redirecting — previously the server-side session termination call was fired with `.catch(() => {})` (auto-expiry) or not awaited (manual logout), so a still-valid server session/cookie could outlive the client's belief that the user was logged out.

### Auth and token scoping

- Login creates a Keystone session; the token is kept server-side, session tracked via cookie.
- OpenStack calls need project- or domain-scoped tokens → route loaders call scoping mutations (e.g. `auth.setCurrentScope` / `rescopeSession`) so components receive already-scoped data.
- A `useScope` hook (commit `c55b535`) combines URL params and auth context on the client.
- **Client auth state** lives in `src/client/store/AuthProvider.tsx` (refactored in #1072): tRPC auth calls are centralized here; `login({ domain, user, password })` and `logout()` come from the context, which also exposes `isLoading`/`error`. A session-expiry timer auto-logs-out and redirects to login with a return-URL param (redirect back after re-login); manual logout does not save a return URL. `LoginForm` uses Juno FormRow/TextInput with uncontrolled inputs.
- Projects list page uses optimistic rendering via Suspense + `useSuspenseQuery` (#1067).
- **Two hooks read scope from different sources, deliberately** — `useDomainId()` (`client/hooks/useDomainId.ts`) reads the domain from the **session** (`useAuth().user.domain.id`), throwing if the user has no domain; `useProjectId()` (`client/hooks/useProjectId.ts`) reads the **URL** (`useParams({ strict: false })`, compatible with both the legacy `/accounts/:accountId/projects/:projectId` and current `/projects/:projectId` shapes), throwing outside a project-scoped route. Don't conflate them: domain scope is session-derived, project scope is URL-derived.
- **Rescope deduplication** (`server/context.ts`): a module-level `Map<authToken, Map<scopeKey, Promise<string | null>>>` (`sessionRescopes`) coordinates concurrent requests from the same session so they don't each hit Keystone. `rescopeSession({ projectId, domainId })` first no-ops if the token is already scoped to the requested project/domain; otherwise it derives a `scopeKey` (`project:{id}` / `domain:{id}` / `unscoped`) and, if another request already has a rescope in flight for that exact key, awaits the same promise instead of re-rescoping — only the resulting **token string** is cached (never the session object), so each request still applies the result to its own `openstackSession`/cookie. If Keystone's rescope response changes the auth token itself, the pending-rescope map is migrated to the new token key (merged with any map already there) before the old key is dropped; a `finally` always removes the completed entry and prunes now-empty maps (both the original and, if migrated, the new key) to avoid a leak. **Fixed in #1209 (merged 27.08.2026):** the cache used to store a *new* promise wrapping the rescope's outcome, so a concurrent request awaiting the cached entry could get a resolved value even when the original rescope had actually rejected — losing the error status. It now caches the original (potentially-rejecting) promise directly, so every awaiter sees the same success/failure. The same PR made `sessionRouter.setCurrentScope` throw `NOT_FOUND` instead of silently returning success when a rescope resolves to `null`, and reworked the `$projectId` route's error UI: an `UNAUTHORIZED` from a failed rescope now shows a "Session Expired" / "Log In" `Status` component instead of the previous, misleading "select a project" message; a `400` is now mapped to `BAD_REQUEST` rather than falling through to the generic `404` handling. Full findings (including what's still below the fix threshold): `prs/1209-multi-tab-auth-error-handling.md`.

## Permissions & policy

Two layers, both driven by oslo.policy files from `policyDir` (JSON/YAML; the reference app ships `compute.json`, `networking.json`, `image.json`, `storage.json`):

1. **policy-engine** (`packages/policy-engine`): lexer → parser (AST) → evaluator; compatible with oslo.policy syntax (`role:admin`, `rule:x`, `user_id:%(target.user.id)s`, `and/or/not`, `@`/`!`, `is_admin:true`…). Works server-side and in-browser. `createPolicyEngine(config)` / `createPolicyEngineFromFile(path)` → `engine.policy(keystoneToken)` → `policy.check("rule", params)`. Debug tracing available.
2. **Permission routers** (see `PERMISSION_ROUTER_IMPLEMENTATION.md`): a generic factory `createPermissionRouter` (`src/server/policies/`) maps UI-facing permission keys to policy engines + rules, with single and bulk checks.

**Permission key convention** (see `PERMISSION_KEY_PATTERN.md`): `scope:resource:action`, e.g. `storage:containers:create`, `network:routers:attach_interface`. Scope is a service domain (`storage`, `network`, `compute` — never implementation names like swift/ceph/neutron); resource is plural snake_case; action is a CRUD verb or specific operation. UI checks these keys, never raw OpenStack rules.

**Ceph/S3 storage permissions** (#1207, merged 31.08.2026): 11 new `storage:*` keys gate Ceph/S3 mutations that previously rendered unconditionally — `storage:containers:update_versioning`, `storage:objects:share`, `storage:object_versions:delete`/`restore`, `storage:container_policies:update`/`delete`, `storage:container_cors_rules:update`/`delete`, `storage:container_lifecycle_rules:update`/`delete`, `storage:credentials:create` (full list in `PERMISSION_KEY_PATTERN.md`). Client-side, `useCephPermissions(projectId)` (`Storage/-components/Ceph/hooks/`) batches all 21 Ceph mutation checks (11 new + 10 pre-existing/newly-wired) into one `storage.canUser` query — a module-level `PERMISSION_MAP` derives both the request array and the `select` re-mapping so they can't desync, `staleTime`/`gcTime: Infinity` since permissions don't change mid-session, and every permission defaults to `false` while loading or on error (fail-closed, same convention as `useSecurityGroupPermissions`). Only mutations are gated — read/list/view/download stay ungated everywhere in the app, now called out explicitly in `PERMISSION_KEY_PATTERN.md`. Two of the eleven new keys are deliberately `rule:storage_viewer` rather than `rule:storage_admin`: `storage:objects:share` (a presigned GET URL just re-exports access a viewer already has) and `storage:credentials:create` (a self-service prerequisite for any Ceph access at all, including read-only browsing). These checks are UX-only — Ceph still enforces real authorization independently via EC2 credentials and bucket policy.

## Client side

### File-based routing (TanStack Router v1)

- Route files live in `packages/aurora/src/client/routes/`; dynamic segments use `$` prefix (`$projectId`), non-route folders use `-` prefix (`-components/`, `-hooks/`, `-modals/`).
- `_auth.tsx` is the shared layout for authenticated views: `beforeLoad` checks/hydrates the session and redirects to `/auth/login` otherwise.
- Loaders (not components) fetch data and set token scope; components receive loader data. Subnavigation is declarative via `linkOptions` + `useParams`.
- Route tree (current): `/` , `/about`, `/auth/login`, `_auth/projects` → `$projectId` → `compute | network (securitygroups, floatingips) | storage ($provider/$storageType/$containerName/objects) | services`. `_auth/accounts` also exists but is a redirect-only stub (`beforeLoad` sends authenticated users with a domain to `/projects`, everyone else to `/`) — no page renders there. `services` itself ships with no built-in child routes since #1189 (24.08.2026, PCA/Clavis removed) — it's an empty OSS-owned layout that only gets content when a consumer passes `additionalProjectServices`.
- **List state lives in the URL.** Toolbar state (filters, search, sort key/direction) for a list view is kept in search params, not component state, so a filtered list is shareable and browser back/forward restores it. The route declares a Zod `validateSearch`; the flavors/floating-IP pattern (#1129) `safeParse`s the whole object and, on failure, falls back **per field** — a bad `sortBy` drops only `sortBy` instead of resetting the toolbar, and the component supplies its own defaults for `undefined`. Navigation intent differs by trigger: sort/filter/immediate-search changes `push` a history entry, debounced search-as-you-type uses `replace` so typing doesn't fill the history stack. Route-specific parse/build helpers live in a route-local `urlHelpers.ts` (floating IPs, security groups), not in shared utils.

### AuroraApp component

`<AuroraApp />` props: `bffEndpoint`, `theme`/`onThemeChange`, `appName`, `slots`, `onTrackEvent`, `enabledServices` (whitelist of service keys: `images`, `flavors`, `securitygroups`, `floatingips`, `containers`, `ceph-containers` — `pca` dropped from the built-in set in #1189, see below), `additionalProjectServices` (#1189, 24.08.2026).

- **Slots** — UI extension points without forking: `logo`, `sideNavBanner` (shadow DOM), `pageFooter`, `login` (for OIDC), `serviceBadge`, `servicePageActions`, `serviceBanner`, `projectsBanner`, `projectOverviewBanner`. Slots get `auroraContext` with a tRPC `client`; service-level slots also get `currentService`. Shadow-DOM slots must inline their styles. `slots.login` replaces the default `LoginForm` on the landing page (`/`) for unauthenticated users — it was silently dropped by the #1072 auth refactor and restored in #1079.
- **`additionalProjectServices`** (`AuroraApp.tsx`, #1189) — the replacement for the removed built-in PCA/Clavis service: consumers register their own project-scoped services as `{ serviceType, serviceName, label, routes }[]`, checked against the project's OpenStack catalog (`serviceIndex[serviceType][serviceName]`) to decide whether the service shows up in the nav/services page. `routes` is a pre-assembled TanStack route (`getParentRoute: () => servicesRoute`, built with `addChildren`) that `createAuroraRouter` (`client/router.ts`) grafts onto the shared `routeTree.gen.ts` singleton at construction time via `parent._addFileChildren({ ...existing, [\`_extra_${i}\`]: route })`. **Known gap, shipped as-is:** this mutates the module-level `routeTree` object itself (not a per-instance copy), keyed only by array index — multiple `createAuroraRouter` calls in the same JS runtime with different `additionalProjectServices` (HMR, multiple app instances, tests) write into the same shared parent node and can cross-contaminate each other's route trees; flagged in review (`prs/1189-…md`) before merge and not addressed. Also still unaddressed at merge: `packages/aurora/README.md` (CLAUDE.md's stated source of truth for `AuroraApp`'s public props) documents neither `additionalProjectServices` nor the `pca` removal — it still lists `"pca"` as a built-in `enabledServices`/service key in two places.
- **Analytics** — `onTrackEvent(payload: {source, action, metadata})`; router navigation is auto-tracked (`source: "router"`, action = route ID, metadata includes pathname/search/section/service from route `staticData`). Custom events go through `useRouteContext().onTrackEvent`. See `packages/aurora/docs/0013_analytics-tracking.md`.

### tRPC client link strategy (`client/trpcClient.ts`)

`getLinks()` is a `splitLink` chain evaluated in order: subscriptions → `httpSubscriptionLink`; non-JSON-serializable input (FormData/Blob/ArrayBuffer) → plain `httpLink` (no batching); JSON procedures listed in the `STREAMING_PROCEDURES` set (currently `storage.swift.downloadObject`, `storage.ceph.objects.downloadObject`) → `httpBatchStreamLink`; everything else → `httpBatchLink`. `httpBatchStreamLink` is deliberately not the default link — it's incompatible with `@fastify/csrf-protection`'s cookie rotation on long-lived connections — so a new streaming procedure must be added to the allow-list explicitly rather than switching the default. A module-level `csrfCache` dedupes concurrent CSRF-token fetches (one in-flight request shared by all callers) and exposes `getCsrfToken`/`setCsrfToken` so a separate JS context — a Web Worker gets its own module instance of `trpcClient.ts` — can share the main thread's cached token instead of re-fetching.

### Query cache isolation

`QueryClient`'s `queryKeyHashFn` (set in `App.tsx`) reads `router.state.matches` directly (not React context) to find the deepest matched route carrying a `projectId` param, and prefixes that project id onto every query key before hashing. Switching projects therefore never returns cached data from a previous project — but only for query keys that go through the shared `queryClient`; ad-hoc caching bypasses this isolation.

## Cross-cutting mechanisms

- **Request cancellation** (`docs/0010_abort_signal_propagation.md`): per-request `AbortController` in Fastify context tied to connection close events (`res.raw`/`req.raw "close"`); signal flows browser → tRPC → signal-openstack → native fetch, so cancelled uploads/queries abort the OpenStack call too.
- **SSE / subscriptions** (`docs/004_proposal_subscriptions.md`): proposed unified single-SSE-connection design (lightweight invalidation notifications + normal tRPC refetch), targeting ~2000 concurrent users; centralized polling worker. Status: proposal.
- **Extensions** (`docs/001_extensions.md`): Aurora Extensions export `./client` and/or `./server` (with `registerRouter`), built from an extension template, sharing tRPC versions via the aurora-sdk concept. The current shipped mechanism for consumers is `createServer({ routers })` + slots.
- **Ceph S3 BFF** (`docs/009_ceph_s3_bff.md`): EC2 credentials fetched via Keystone, AWS SDK v3 with path-style addressing, S3 errors mapped to tRPC error codes, project-scoped isolation; needs `cephRegion`. Object upload (`uploadObject`, #1086) streams the request body straight into a `PutObjectCommand` via `octetInputParser` (no multipart, no whole-file buffering) with per-chunk progress on a `watchUploadProgress` subscription; it runs on a dedicated `cephUploadProcedure` rather than the usual `cephProtectedProcedure`, because tRPC can't merge a raw-stream (`octetInputParser`) input with the object-shaped `project_id` input that `projectScopedProcedure`-based procedures bundle ("All input parsers did not resolve to an object"). So upload metadata (project id, bucket, object key, size, an `x-upload-id` correlating with the progress subscription) travels as `x-upload-*` request headers instead of a tRPC input, and `cephUploadProcedure` rescopes the session from the `x-upload-project-id` header itself. Sharing an object goes through `objects.generatePresignedUrl` (#1120, a normal `cephProtectedProcedure`): `getSignedUrl` from `@aws-sdk/s3-request-presigner` signs a `GetObjectCommand` with the request's EC2 credentials — the S3 answer to Swift temp URLs, but with no key to configure and no round-trip to Ceph, since signing is purely local. It returns `{ url, expiresAt }` (absolute unix seconds, so the UI shows a real expiry rather than a duration), and `expiresIn` is capped in the Zod input schema at `S3_PRESIGN_MAX_EXPIRY_SECONDS` (604800 = 7 days, `Storage/constants.ts`) because the SigV4 signer rejects anything longer.
- **Virtualized storage tables** (`client/hooks/useAvailableViewportHeight.ts`, `useVirtualizedTableBody.ts`, #1090 + #1123): shared hooks size Ceph/Swift bucket and object tables to the actual remaining viewport height instead of letting `max-height` follow content — the latter creates a feedback loop where measured rows resize the scroll container, which makes `@tanstack/react-virtual` recompute its range and re-measure. **Both** edges are measured, in document coordinates (`getBoundingClientRect().top + scrollY`, so the value doesn't drift with scroll): the top from the element itself, so a banner slot, wrapped toolbar or second breadcrumb line shrinks the table rather than growing the page; the bottom from the page footer's top edge, found by `document.querySelector(".app-page-footer")` — an app-owned wrapper added around the `pageFooter` slot in `__root.tsx` deliberately *not* a component-library class, so the measurement survives changes to the shell's internal markup. The footer is a sibling, not an ancestor, so it's explicitly added to the `ResizeObserver` alongside the element's ancestor chain; a custom footer of any height therefore re-triggers measurement instead of overlapping the last rows (short-footer gap / tall-footer overlap was the #1123 bug). With no footer at all (embedded mode, no footer slot) it falls back to viewport bottom minus a 52px constant. Clamped to a **150px** floor (was 200 before #1123) so a very short viewport falls back to page scroll rather than an unusably short table. The per-call `bottomGap` parameter of both hooks was removed in #1123 — the footer is measured, not configured. `useVirtualizedTableBody` withholds `virtualItems`/`totalSize` until the height is known, so the virtualizer never renders the whole unvirtualized list on first paint.
- **Ceph and Swift object downloads run in a Web Worker** (#1062 Ceph, #1155 Swift — merged 2026-08-13, same pattern; `client/routes/_auth/projects/$projectId/storage/-components/{Ceph,Swift}/Objects/`): streaming, base64 decode, and Blob assembly for `storage.ceph.objects.downloadObject`/`storage.swift.downloadObject` happen off the main thread in each backend's own `workers/objectDownload.worker.ts`, so a large object doesn't jank the UI. Transfer ownership lives in a module-scope store (`stores/objectDownloadStore.ts`, outside React, read via `useSyncExternalStore`) rather than in `ObjectsTableView` — a download survives the table unmounting on folder navigation or a spinner swap, matching what the "download started" toast already promises. Concurrent transfers share one persistent toast per backend (separate toast-ids so the two backends don't dismiss each other's), dismissed when the last one ends (saved/failed/cancelled). Cancel is cooperative: the row clears immediately, the worker is told to abort its fetch (which tears down the BFF's read via `ctx.req.signal` — S3 for Ceph, the Swift stream for Swift — no separate server-side cancel procedure), and is force-terminated after a 5s grace period if it never reports back. Swift tracks progress per-row via a `watchDownloadProgress` subscription; Ceph's original implementation used a single shared in-flight flag instead. Full analysis, including findings that shipped with #1155 unfixed: `DOCS/aurora-dashboard-kb/prs/1155-swift-object-download-web-worker.md`.
  - The worker is bundled inline (Vite `?worker&inline`), not as a separate asset — a URL-referenced worker would be lost when a consuming app re-bundles the library. Inline means it runs from a `blob:` URL, which needs `worker-src 'self' blob:` in CSP; `createServer()`'s production Helmet config now sets this (`server.ts`) — a consumer running its own CSP needs the same directive.
  - A worker gets its own module instance of `trpcClient.ts`, so it never sees `App`'s `setBffEndpoint()` call or the main thread's CSRF token cache — `getBffEndpoint()`/`getCsrfToken()`/`setCsrfToken()` were added to hand both across explicitly (BFF endpoint must be absolute for the worker, since a `blob:` location can't resolve a root-relative path).
  - The library build's `vite.config.mjs` substitutes `process.env.NODE_ENV` via `define` — react-query (pulled in by `trpcClient`) reads it, and the inlined worker has no `process` to read it from at runtime.
- **Toasts go through `NotificationManager`, not the legacy `<Toast>`** (#1132 migrated the last holdout, Glance images; Swift/Ceph storage views already did): a view no longer holds toast state or renders a `<Toast>` element. Per-domain builder modules (e.g. `ImageToastNotifications.tsx`, `ObjectToastNotifications.tsx`) export `getXxxToast(...)` functions returning `{ message, ...NotificationOptions }`; the call site destructures and picks severity itself — `const { message, ...options } = getImageUpdatedToast(name); toast.success(message, options)`. Severity lives at the call site, not in the builder, and `setToastData`-style plumbing is gone. Error-path builders take an already-extracted message string, so callers must read `error.data?.path` / error messages null-safely — otherwise a failure throws inside its own `catch`. Note #1132 left `NotificationText.tsx` (+ its test) orphaned in the images folder — dead code, not a dependency. Remaining coverage gaps were closed by #1221 and #1239 (merged 31.08–01.09.2026): new builder modules for security groups/RBAC policies (`SecurityGroupToastNotifications.tsx`), flavors (`FlavorToastNotifications.tsx`) and floating IPs (`FloatingIpToastNotifications.tsx`); `BucketPolicyModal` (Ceph) gained a third `action: "saved" | "deleted"` parameter so `BucketModals.tsx` can pick the right save-vs-delete toast.
- **Gardener**: mentioned as an integration direction (Kubernetes-native infra management) — branches exist upstream, not part of the current core routers.
- **Shared two-column detail view** (`client/components/TwoColumnDescriptionList.tsx`, #1111): data-driven replacement for hand-rolled `DescriptionList`/`DescriptionTerm`/`DescriptionDefinition` JSX pairs — takes `items: {id?, label: string | ReactNode, value: string | number | ReactNode | undefined}[]`, splits them into two `DescriptionList` columns. Used by Flavor, Image, Floating IP and Security Group detail views; moved from the floating-IP-only `-components/` folder up to the shared `client/components/` once other domains adopted it.
- **Juno `Status` component is now the standard loading/error state** (#1222, merged 01.09.2026, building on the juno-ui-components 9.4.0 bump from #1209 — see 01-overview.md): `RouteError` (`client/components/Error/RouteError.tsx`) was rewritten from a hand-rolled `ContentHeading`+`<p>` layout to `<Status status="error" title=… body={errorMessage + " " + helpText} action={<Button onClick={() => navigate({ to: "/" })}>Go to Home</Button>} />`. The same component now backs loading and error states across nearly every list/detail route — Flavors, Images, Instances, KeyPairs, ServerGroups, Floating IPs, Security Groups, Ceph Buckets/Objects, Swift Containers/Objects, the projects index and the landing page — replacing per-view custom `Spinner`+`Stack` loading blocks and bespoke error `<div>`s (net ~120 lines removed across ~26 files, changeset `smooth-lions-rest`). A new list/detail route should reach for `Status` rather than rolling a custom loading/error layout.
- **Deletion/confirmation modals standardized on TanStack Form + Zod** (#1211, merged 01.09.2026, changeset `swift-ceph-delete-modals`): 15 delete/detach/release/remove modals across Compute (Flavors, Images), Network (Floating IPs, Security Groups/RBAC policies) and Storage (Swift, Ceph) moved from ad-hoc `useState`-driven confirm-text checks to `@tanstack/react-form`'s `useForm`/`useStore` with a Zod `refine` schema (typically `z.string().refine(v => v === "delete", …)`) driving the confirm button's disabled state — plus the pre-existing `useModalTracking` hook (`client/hooks/useModalTracking.ts`, since #1007) for `.open`/`.close`/submit analytics, replacing one-off tracking calls. There is no shared `useDeleteConfirmation` hook (an earlier, unmerged version of this PR had one — see `prs/1211-…md` — but the merged version keeps each modal's own `useForm` call against the same schema shape). A new deletion modal should follow the TanStack Form + `useModalTracking` pattern, not the old manual-`useState` one.
- **Shared `ContentHeader`** (`client/components/ContentHeader/ContentHeader.tsx`, since #779): page header combining title, a project-ID clipboard chip, optional description, and slot banners/actions (`servicePageActions`, `serviceBanner`, `projectOverviewBanner`). #1242 (merged 01.09.2026) extended its adoption to Floating IPs and Security Groups, replacing page-local `ContentHeading`+`Breadcrumb` pairs and the dedicated `SecurityGroupHeader` component (deleted) — the separate persistent breadcrumb (`ProjectInfoBox` in `$projectId.tsx`) is a different mechanism and was untouched by this PR. Flavors and Images already used `ContentHeader`.
