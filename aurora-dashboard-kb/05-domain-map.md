# 05 — Domain & Feature Map

Where each feature lives: server router (`packages/aurora/src/server/…`), client routes (`packages/aurora/src/client/routes/_auth/…`), and the design doc in `packages/aurora/docs/`.

## Domains

| Domain | Server routers | Client UI | Design doc |
| --- | --- | --- | --- |
| **Authentication** | `Authentication/routers/sessionRouter.ts` | `/auth/login`, `_auth.tsx` guard | — (see 02-architecture) |
| **Project** | `Project/routers/projectRouter.ts` | `/projects`, `/projects/$projectId` overview; each service card's group label lost its `expandMore` chevron icon (#1289, merged 11.09.2026) — it never did anything (no expand/collapse behavior behind it) | — |
| **Compute — servers** | `Compute/routers/serverRouter.ts`, `serverGroupRouter.ts` | project compute section | — |
| **Compute — flavors** | `Compute/routers/flavorRouter.ts` | flavors list/detail (admin CRUD, metadata, access control); components live under `compute/flavors/-components/`, moved from the old shared `compute/-components/Flavors/` (#1245, merged 01.09.2026); mutation toasts via `FlavorToastNotifications` builders (#1239); **both Flavor modals use a draft + batch-save pattern since #1247** (merged 02.09.2026): `EditSpecModal` (extra specs) and `ManageAccessModal` (tenant access) hold a local draft, show inline row editing, and commit everything on "Save Changes" instead of firing a mutation per row — `TenantAccessFormRow`/`TenantAccessRow` were deleted. Two things shipped as-is and are worth knowing before touching these: the batch save runs **deletes before creates with no rollback and no refetch**, so a failure on the create half leaves the specs/access list partially destroyed (verified still true at this pin, `EditSpecModal.tsx` `handleSaveAll`), and the changeset's "confirm-delete with a 3-second timeout" belongs to `ManageAccessModal`, not `EditSpecModal` (see `prs/1247-…md`); empty list state uses Juno `Status` inside the grid since #1256; detail page has a single primary action since #1278 (merged 05.09.2026) — `Edit Metadata` is primary, `Manage Access`/`Delete` moved into a more-actions dropdown, and the list view's button spacing was adjusted to match; sort-option labels now match the table headers exactly, including units (`VCPUs`→`vCPU`, `RAM`→`RAM (MiB)`, `Root Disk`→`Root Disk (GiB)`, `Swap`→`Swap (MiB)`, #1289, merged 11.09.2026); three TilmanHaupt PRs (#1303 merged 16.09, #1311 merged 17.09, #1313 merged 18.09.2026) replaced the #1247 batch-save pattern on both flavor modals: `EditSpecModal` now mutates immediately per add/edit/delete with a toast each time (keys read-only once created, no Cancel button — Close only), gated behind `flavor_specs:list`+create/delete permissions instead of always rendering; the view-only "Metadata" trigger is gone from the list menu — metadata is always visible in `FlavorDetailsView`'s Details section, with a separate Edit button shown only when the caller can manage specs. `ManageAccessModal` got the same immediate-mutation treatment plus a persistent `Message` validation banner (was inline `errortext`); "Manage Access" now **hides** rather than disables for a public flavor unless the caller actually has `add_project`/`remove_project` (previously shown-disabled for anyone with mere `list_projects`) — this also closes the gap the #1303 report flagged against `$flavorId.tsx` (verified fixed on current `origin/main`: `hasPopupMenuItems = (canManageAccess && !isPublicFlavor) || canDeleteFlavor`). Create Flavor modal reorganized into two `FormSections` ("Flavor Information"/"Hardware Specifications") with helper text; detail page mirrors the two-column split, `Disk` label renamed `Root Disk` | `003_flavors_ui_requirements.md` |
| **Compute — images** | `Compute/routers/imageRouter.ts` | images UI (list/search/visibility/members/metadata; excluded props via env); components live under `compute/images/-components/`, moved from the old shared `compute/-components/Images/` (#1245, merged 01.09.2026); notifications go through the app-wide `NotificationManager` `toast` API via `ImageToastNotifications` builders, not the legacy `<Toast>` (#1132), extended to cover update/activate/deactivate/visibility-change actions that were previously silent (#1239) — see 02-architecture "Cross-cutting mechanisms". **Server-side pagination since #1248** (merged 02.09.2026): `listImagesWithPagination` takes a `marker` and returns `totalCount` alongside `images`/`next` (`imagesPaginatedResponseSchema`, `types/image.ts`). The title "add pagination to images" undersells what the diff does to the router — it **deleted the early-exit fetch strategy**. Before, the procedure fetched one 100-item Glance page (or, with a search term, kept paging only until 50 post-filter matches accumulated, `MIN_RESULTS_WHEN_SEARCHING`); now it always walks **every** Glance page from the start (`OPENSTACK_PAGE_SIZE` 100, `MAX_PAGES_TO_SEARCH` 1000 as the only stop) on every request, because `totalCount` has to be exact for the page counter — so cost per list call now scales with the whole image catalogue, not the page size. Partly offset by pushing single-value filters (`status`/`visibility`/`disk_format`/`container_format`/`protected`/`owner`) into the Glance query; multi-value (`in:a,b`) filters and name search stay BFF-side. Marker slicing is done in memory (`findIndex(img.id === marker) + 1`), and the loop now runs each `next` link through the pre-existing `validateRelativeUrl` helper (`server/helpers/urlValidation.ts`) before following it — SSRF guard on an OpenStack-supplied URL, matching what the non-paginated branch already did. Client: member-status views (`pending`/`accepted`) are **not** server-paginated and still slice client-side; `List.tsx` distinguishes the two modes by whether `totalCount` is present. **#1260** (merged 03.09.2026) then reversed the cache strategy for image mutations: `utils.compute.listImagesWithPagination.invalidate()` was removed from delete/create/activate/deactivate/update/visibility mutations and replaced by optimistic local overrides pushed through `onImageUpdated` (plus one `onMemberStatusChanged()` refetch after create) — its changeset says it "triggers proper list refresh after image mutations", which is the opposite of what the diff does; the list no longer refetches on those mutations at all. `ActivateImageModal`'s info `Message` also became a plain `<p>`; detail page likewise reduced to one primary action since #1278 (merged 05.09.2026) — `Edit Details` is primary, `Edit Metadata` moved into the more-actions dropdown. **#1283** (merged 08.09.2026): image modal UX pass — bulk action modals (delete/deactivate/activate) now show image names instead of UUIDs and put the count in the modal title via `Plural`; exception sub-lists (protected/already-active/already-deactivated) moved above the main action list; `EditImageMetadataModal` reverted from `xl` back to `large` sizing. Sort options dropped `Updated At` and renamed `Name` → `Image Name` (#1289, merged 11.09.2026), matching the table header; **#1303** (merged 16.09.2026): `EditImageMetadataModal` lost its duplicate delete-confirmation step (single-click delete) and switched title/footer between "Edit Metadata"/"Show Metadata" based on `canEdit` (Save Changes hidden when read-only), grew to `xl`, and dropped the metadata list's header row; image sharing's "Member ID" field relabeled "Project ID" (#1296); Flavor/Image popup menus sorted alphabetically with Delete separated by a divider (#1302). **Known gap (report [80]):** the new read-only `canEdit=false` mode is unreachable — both call sites into the modal only open it from an already-`permissions.canUpdate`-gated action, so the read-only branch has no live caller | `005_images_bff.md` (full BFF API reference, Glance v2) |
| **Compute — keypairs** | `Compute/routers/keypairRouter.ts` | — | example in `docs/aurora_architecture_overview.md` |
| **Network — security groups** | `Network/routers/securityGroupRouter.ts`, `securityGroupRuleRouter.ts`, `rbacPolicyRouter.ts` | `network/securitygroups`, `…/$securityGroupId` (+AddRuleModal); actions are permission-gated via `useSecurityGroupPermissions` (`network:security_groups:*`, `network:security_group_rules:*`, `network:rbac_policies:*` keys, one bulk `canUser` check), list toolbar (sort/filter/search) syncs to URL via `urlHelpers.ts` (#952); detail page header uses the shared `ContentHeader` component instead of the now-deleted `SecurityGroupHeader` + local `Breadcrumb` (#1242, merged 01.09.2026); security-group/RBAC-policy mutations get toasts via `SecurityGroupToastNotifications` (#1221, merged 31.08.2026); #1254 (02.09.2026) added a `network/securitygroups.tsx` layout route carrying the section's static breadcrumb, and the detail route sets its own crumb at runtime via `useSetBreadcrumb`; the list's empty state moved from a bare `<Trans>There are no groups</Trans>` early return to a Juno `Status status="empty"` row inside the grid, so headers survive an empty filter result (#1256, 03.09.2026) **#1286** (merged 10.09.2026, KirylSAP): detail page reached the #1278 single-primary-action pattern — `Edit Details` is now the primary button (moved out of `SecurityGroupBasicInfo`, which lost its own inline Edit button), with a `More Actions` kebab holding `Delete Group`; groups owned by another project (shared read-only copies) disable both. Deletion is newly wired end-to-end (`network.securityGroup.deleteById` mutation, toast, list invalidation) — the confirm dialog (`DeleteSecurityGroupDialog`) already existed but wasn't hooked up to a working mutation before. `SecurityGroupBasicInfo`'s `Stateful` field now defaults to `Yes` when the API omits the field, matching Neutron's own default (previously showed `No`). A new BFF-side **`stateful` filter** (`filterSecurityGroupsByStateful`, treats a missing field as `true`) joins the existing `shared` filter on the list toolbar, both built through a new route-local `filterConfig.ts` that types filter params against the tRPC input schema at compile time (`SecurityGroupFilterParams = Pick<ListSecurityGroupsInput, "shared" | "stateful">`) rather than the generic `Record<string, string>` the other domains' toolbars use; both filters render human labels ("Yes"/"No") for their raw `true`/`false` values via a new generic `valueLabels`/`resolveValueLabel` mechanism added to the shared `ListToolbar` (`components/ListToolbar/valueLabels.ts` — deliberately uses `Object.hasOwn` instead of a plain lookup so a URL-supplied value like `__proto__` can't resolve to an inherited `Object.prototype` member and slip past a `??` fallback). RBAC policy row menu label clarified `Remove` → `Remove Policy`. Same PR closed two of the three `validateAndEncodeResourceId` gaps on `rbacPolicyRouter`/`securityGroupRuleRouter` and removed the dead `network:security_group_rules:update` permission key — see 02-architecture "Path & URL validation" and "Permission key convention" | `007_security_groups_bff.md` (Neutron v2, CRUD + rules + RBAC) |
| **Network — floating IPs** | `Network/routers/floatingIpRouter.ts` (`network.floatingIp.*`; `searchTerm` matches `description`, `floating_ip_address`, `fixed_ip_address`, `floating_network_id`) | `network/floatingips`, `…/$floatingIpId`; list header uses `DataGridToolbar` (`SortInput`/`FiltersInput`/`SelectedFilters` + 500ms-debounced `SearchInput`), `placeholderData` keeps the previous rows on screen during refetch so only the initial load blocks on a spinner (a non-blocking `Message` banner covers refetch errors when cached rows exist); "Allocate Floating IP" is gated by `network.canUser` (`network:floatingips:create`) instead of a hardcoded permission; bulk selection/action UI was removed as unused (#1099); filters/search/sort are persisted in URL search params via the route's `validateSearch` + route-local `urlHelpers.ts`, with per-field `safeParse` fallback and push-vs-replace history semantics (#1129); detail page header uses the shared `ContentHeader` component instead of a local `ContentHeading`+`Breadcrumb` pair (#1242, merged 01.09.2026); mutation toasts via `FloatingIpToastNotifications` (#1239, merged 01.09.2026); **`create` derives `tenant_id`/`project_id` from the session token, not client input** (#1206, merged 26.08.2026, see 02-architecture "Session token exposure & ownership-confusion fixes") — closes an ownership-confusion gap where a caller could allocate an IP attributed to a `project_id` other than the one their session was actually scoped to; #1254 (02.09.2026) added a `network/floatingips.tsx` layout route holding the section's static breadcrumb (``msg`Floating IPs` ``) with the detail route registering the IP itself dynamically; empty list state switched to a Juno `Status status="empty"` row inside the grid (#1256, 03.09.2026) | `008_floating_ips.md` |
| **Storage — Swift** | `Storage/routers/swift/swiftRouter.ts` | `storage/$provider/$storageType/…/objects`; object downloads/previews run in a Web Worker off the main thread, at parity with Ceph (#1155, merged 2026-08-13), see 02-architecture "Cross-cutting mechanisms" and `prs/1155-…md` for findings shipped unfixed; account/container metadata redacts TempURL and sync secrets to presence flags, and TempURL generation is capped to read-only `GET` with a 60s–7d lifetime (#1173, #1176, merged 14.08/17.08.2026 — see 02-architecture "Secret redaction & TempURL hardening" and `prs/1173-…md`, `prs/1176-…md`). **In-container page reached header parity with Ceph in #1243** (merged 02.09.2026): new `Swift/Containers/ContainerHeader.tsx` + `ContainerHeaderActions.tsx` render a `ContentHeader` with an overflow menu (Manage Access · Preview and Edit metadata · Empty Container · Delete Container) mirroring `Ceph/Buckets/BucketHeader`, chosen in `…/$containerName/objects/index.tsx` by `provider === "swift"`. It reuses the four container-list modals but feeds them **real** metadata from `storage.swift.getContainerMetadata` (one HEAD, input kept as exactly `{ project_id, container }` so it shares the cache entry with the modals' own identical queries) rather than a `{count: 0, bytes: 0}` placeholder — three of the four modals genuinely read `count`/`bytes`. Deleting from that page now navigates back to the container list instead of stranding the user on a dead route. Unlike Ceph's, the menu has **no permission gating** — Swift has no `canUser`/policy wiring in this area at all (`TODO(perms)` in `Swift/Containers/index.tsx`), so all four items always show. The same PR renamed Swift action labels to be self-describing across row menus and their confirm buttons (`Delete` → `Delete Object`/`Delete Container`, `Copy` → `Copy Object`, `Move/Rename` → `Move/Rename Object`, `Edit Metadata` → `Edit Object Metadata`, `Share URL` → `Share Object URL`, `Download` → `Download Object`; `DeleteObjectsModal` pluralises its confirm label via `plural()`) and reorganised the objects toolbar — `Upload Object` is now the primary button, `Create Folder` moved into a new `moreVert` overflow menu. Follow-ups on the containers info strip: `ContainerLimitsTooltip` got `max-h-5` on its `TooltipTrigger` so the native-button trigger matches the 20px icon (#1251, merged 02.09.2026), then lost its `Containers` count row entirely because the eventually-consistent account-metadata count disagreed with the container listing shown next to it (#1257, merged 03.09.2026); `EditContainerMetadataModal`'s inline per-row delete button went from `primary-danger` to `default`, matching the object metadata editor (#1259, 03.09.2026); listing timestamps (`last_modified`) are UTC without a zone suffix — `parseSwiftDate`/`formatSwiftDate` (`client/utils/formatSwiftDate.ts`, #1270, merged 04.09.2026) normalize them before parsing so table display and sort order match local time (both container/object tables and their sort comparators; Ceph is unaffected, its timestamps carry `Z`). **Known regression at this pin:** the same normalization also fires on the raw HTTP `Last-Modified` header (contains the substring `GMT`, which contains `T`), so `EditObjectMetadataModal`'s "Last modified (UTC)" field now always renders `—` — see `prs/1270-…md`; sort option `Name` renamed to `Container Name` to match the table header (#1289, merged 11.09.2026); **#1288** (merged 14.09.2026): `CreateContainerModal` gained S3-style name validation (length/charset/IP-shaped-name/reserved prefix-suffix checks, shared with Ceph's bucket validator) and its own `CreateFolderModal` gained a client-side duplicate check; `createContainer` no longer does a HEAD-then-PUT race — it branches on the container-PUT's own idempotent status (`201` created / `202` already existed) and now resolves `{ created: true, optionsApplied: boolean, optionsError?: string }` instead of `boolean` (**breaking**, major changeset `full-maps-obey`) so a failed metadata/ACL/quota follow-up doesn't masquerade as a failed create; `createFolder`'s marker write became an atomic `If-None-Match: "*"` create-if-not-exists (throws `CONFLICT` on an existing folder), incidentally fixing a pre-existing arg-order bug that stored the marker as a ~90-byte JSON blob instead of a proper zero-byte `application/directory` object; non-conflict create errors now show a persistent inline banner instead of an auto-dismissing toast, modal stays open for retry | `006_swift_object_storage_bff.md` (verified against Swift API; relative paths, SDK handles /v1/AUTH_) |
| **Storage — Ceph S3** | `Storage/routers/ceph/`: `containerRouter`, `objectRouter`, `bucketPolicyRouter`, `versioningRouter`, `ec2CredentialRouter`, `corsRouter` (#1092, merged 12.08.2026 — was already live at the current pin but missing from this row), `lifecycleRouter` (#1178, merged 27.08.2026) | Ceph buckets/objects components; object downloads/previews run in a Web Worker off the main thread (#1062); object upload via file picker/drag-and-drop with progress + cancel (#1086, `UploadObjectModal`), see 02-architecture "Cross-cutting mechanisms"; `objects.generatePresignedUrl` + `GeneratePresignedUrlModal` issue a time-limited shareable GET link (#1120); bulk delete of objects/versions via multi-select + `DeleteObjectsModal` (#1121, merged 2026-08-07) — chunked `DeleteObjects` (max 1000 keys/call); **known bug:** `RestoreVersionModal`/`DeleteVersionModal` swallow `errorCount` from `deleteVersionsBulk` and report success even on partial S3 failure (fix planned, not yet implemented — `DOCS/plans/2026-08-03-fix-deleteversionsbulk-partial-failure.md`); `ec2CredentialRouter.delete` verifies ownership (`user_id`+`project_id` from the session token match the credential's) before deleting, returning `NOT_FOUND` rather than `FORBIDDEN` on mismatch to avoid confirming a credential ID exists to someone who doesn't own it — closes an IDOR (#1182, merged 24.08.2026, see 02-architecture "Cross-cutting mechanisms"); **S3 lifecycle configuration** (#1178, merged 27.08.2026): `lifecycleRouter` `get`/`set`/`delete` manage per-bucket lifecycle rules (expiration by days/date, noncurrent-version expiration/transition, storage-class transitions, abort-incomplete-multipart, prefix/tag filters) via `lifecycleMapper.ts` (`Storage/helpers/`) mapping between the Zod schemas in `Storage/types/ceph.ts` and S3 lifecycle XML; `get` parses each rule individually against a lenient read schema and skips (rather than fails on) a rule authored outside Aurora, returning `skippedRuleCount` so the client can warn and disable mutations rather than erroring the whole page. UI: `LifecycleRulesTab`/`LifecycleRulesTable`/`LifecycleRuleForm`/`LifecycleRuleModal` + delete modals, same tab-based pattern as the CORS rules UI (#1092); all 21 Ceph mutation actions (bucket/object/version/CORS/lifecycle/policy/credential) are now permission-gated client-side via `useCephPermissions` (#1207, merged 31.08.2026 — see 02-architecture "Permissions & policy"), and `BucketPolicyModal` toasts now distinguish save vs delete via an `action` param (#1221, merged 31.08.2026); **`EmptyBucketModal` is now the authoritative live check** (#1249, merged 02.09.2026): both of its queries drop to `staleTime: 0` (overriding the app-wide 60s default in `App.tsx`) and the version/delete-marker query runs unconditionally instead of only for versioned buckets, `calculateBucketState` is called with `0` rather than the possibly-stale `bucket.count` from the list cache, and a genuinely empty bucket gets a distinct info-only branch ("This bucket is already empty", Close-only footer) instead of the destructive confirm form — matching Swift's Empty Container behaviour, with the row action staying visible either way. Because `objects.list` only returns the first `maxKeys: 100` page, an `isTruncated` response is treated as *not* classifiable and falls back to the normal destructive form rather than guessing a safe branch; sort option `Name` renamed to `Bucket Name` to match the table header (#1289, merged 11.09.2026); **#1288** (merged 14.09.2026): `CreateBucketModal`/Ceph `CreateFolderModal` share the same S3 name-validation rules Swift now uses (length/charset/IP-shaped-name/reserved prefix-suffix); Ceph `createFolder`'s write is now an atomic `IfNoneMatch: "*"` create-if-not-exists, throwing `CONFLICT` on an existing key instead of resolving successfully — **breaking** (major changeset `full-maps-obey`) — and its return type simplified from `{ success: boolean }` to plain `boolean`; non-conflict create errors now show a persistent inline banner instead of an auto-dismissing toast | `009_ceph_s3_bff.md` (EC2 creds via Keystone, AWS SDK v3, error mapping, upload/download streaming, presigned URLs, S3 lifecycle configuration) |
| **Services — extensibility** | — (no built-in server router; was `Services/routers/pcaRouter.ts`, removed #1189) | `_auth/projects/$projectId/services/$serviceType/**` — an empty OSS-owned mount point by default. Since #1268 (merged 04.09.2026, replacing #1189's route-injection design after 11 days) a consumer registers a self-contained `{ serviceType, serviceName, label, component }` via `serviceExtensions` on `<AuroraApp />`; OSS mounts `component` at that URL and hands it `{ basePath, context }`, and the extension owns its own router/header rather than having routes grafted into Aurora's tree (see 02-architecture "AuroraApp component"). Known gap: an unregistered `serviceType` renders a blank content area instead of a 404. PCA/Clavis (CA list/create/delete/import chain, certificates, lifecycle actions) was the one built-in example of this domain until #1189 (merged 24.08.2026) deleted the entire server + client implementation (~7.5k lines) in favor of the consumer-registered pattern | `0011_clavis.md` now describes a removed feature — stale, not updated or deleted by #1189 or #1268 |
| **Permissions** | `policies/createPermissionRouter.ts` + per-domain `permissionRouter.ts` (Compute, Network, Storage) | UI checks `scope:resource:action` keys | `PERMISSION_KEY_PATTERN.md`, `PERMISSION_ROUTER_IMPLEMENTATION.md` (repo root — accidentally deleted by PR #1146, restored by PR #1158, see 01-overview.md gotchas) |

## Design doc index (`packages/aurora/docs/`)

| Doc                                  | Topic                                                                                  | Status flavor          |
| ------------------------------------ | -------------------------------------------------------------------------------------- | ---------------------- |
| `001_extensions.md`                  | Aurora Extension model (client/server exports, registerRouter, aurora-sdk, tRPC rules) | concept                |
| `002_integration_approaches.md`      | Embedding Aurora in the legacy (Elektra) dashboard: iframe vs alternatives             | analysis               |
| `003_flavors_ui_requirements.md`     | Flavors control requirements (business + functional, admin-only CRUD)                  | requirements           |
| `004_proposal_subscriptions.md`      | SSE real-time updates architecture (single connection, invalidation events)            | proposal               |
| `005_images_bff.md`                  | Glance Images BFF API reference (endpoints, schemas, params)                           | implemented, reference |
| `006_swift_object_storage_bff.md`    | Swift BFF (accounts/containers/objects, versioning notes)                              | implemented, verified  |
| `007_security_groups_bff.md`         | Security Groups + Rules + RBAC BFF                                                     | implemented, verified  |
| `008_floating_ips.md`                | Floating IP BFF + mutations hook                                                       | implemented            |
| `009_ceph_s3_bff.md`                 | Ceph RGW S3 BFF (EC2 creds, S3 client, error mapping, S3 lifecycle config since #1178) | implemented, reference |
| `009_playwright_e2e_testing.md`      | E2E smoke test plan (2 phases)                                                         | implemented            |
| `0010_abort_signal_propagation.md`   | AbortSignal propagation browser→OpenStack                                              | implemented, reference |
| `0011_clavis.md`                     | Clavis/PCA integration scope + UI                                                      | **stale** — describes the built-in implementation removed in #1189 (24.08.2026); not updated or deleted by that PR |
| `0012_aurora-pr-preview-workflow.md` | PR preview: labels `pr-build`/`pr-preview`, GHCR, ArgoCD                               | implemented            |
| `0013_analytics-tracking.md`         | Analytics: onTrackEvent, router auto-tracking, semantic actions                        | implemented, guide     |
| `0014_dependency_classification.md`  | `dependencies` vs. `peerDependencies` rule for React-using packages, detection checklist | guide                  |

Note: older docs (005–008) reference historical paths `apps/aurora-portal/...` — the code now lives in `packages/aurora/src/...`; the API descriptions remain accurate.

## Where to add a new feature (checklist)

1. **Types**: Zod schemas in `packages/aurora/src/server/<Domain>/types/`.
2. **Router**: tRPC router in `<Domain>/routers/`, built with `auroraRouter`/`protectedProcedure` (or scoped procedures); mount in `<Domain>/routers/index.ts` and the root router.
3. **Permissions**: add `scope:resource:action` keys via the permission router factory; policy rules in the consumer's policy files (`apps/dashboard/src/policies/*.json`).
4. **Client route**: file-based route under `routes/_auth/projects/$projectId/...`; loaders fetch + scope, `-components/`/`-hooks/`/`-modals/` for non-route code; add `staticData` (section/service) for analytics.
5. **i18n**: wrap strings with Lingui; run `pnpm check-i18n` (en + de catalogs).
6. **Tests**: colocated `*.test.ts(x)` (vitest); e2e spec in `apps/dashboard/e2e/` if user-facing.
7. **Changeset + conventional commit** (scope from allowed list), PR with template; label `pr-build` for a preview environment.
