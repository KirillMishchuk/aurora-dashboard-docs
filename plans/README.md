# Plans

| Date | Task | Risks | Status | File |
| --- | --- | --- | --- | --- |
| 2026-09-25 | Четвёртый раунд замечаний Copilot на [PR #1331](https://github.com/cobaltcore-dev/aurora-dashboard/pull/1331) (ветка `kiryl-s3-version-pagination`, HEAD `03144db3`) — 7 замечаний, **ни одно не чинится в этом PR**. **Три валидных → follow-up 8–10.** (3) `objectRouter.ts:929`: при `IsTruncated: true` без `NextKeyMarker` отсрочка группы не срабатывает и последняя группа страницы удаляется целиком — опасен только случай, когда её current-запись delete-маркер: за границей страницы остаётся более старая версия, становится current, удалённый объект воскресает живым; правку связывает намеренный тест `objectRouter.test.ts:1469`, закрывающий противоположный риск, оба требования совместимы. (4) `objectRouter.ts:989`: `bulkDeleteItems` судит о systemic-сбое по своим локальным массивам, а зовётся постранично — падение на второй странице реджектит мутацию, и ни `deletedCount` прошлых страниц, ни `isPartial` до пользователя не доходят. (6) `ObjectBrowserView.tsx:358`: вкладка Deleted fail-open — при ошибке, при загрузке и **на любом Suspended-бакете** (`enabled` запроса требует `Enabled`, а вкладка рендерится при `!== "Unversioned"`) показывает все папки; разрушающих действий у них нет, сливается с follow-up 1 и находкой 1 раунда 3. **Одно частично валидное → follow-up 11:** Empty Bucket запускает удаление версий без `storage:object_versions:delete`, но это не регрессия PR — те же ворота стоят на `BucketTableView.tsx:255` и на `main`, а серверной проверки прав на Ceph-процедурах нет вовсе (`cephProcedure.ts:107`). **Два ложных срабатывания:** abort-гонки в `containers.getState` (`:296`) и `checkDeletedContent` (`:470`) — если последняя страница доехала и она не truncated, скан дошёл до конца, `isPartialScan: false` правда; плюс ответ отменённого запроса никому не доставляется. **Одно отклонено:** безусловная инвалидация в `invalidateBucketQueries` — решение раунда 2, задокументированное в `:26-30` и `:55-68`; довод про `EditMetadataModal` бьёт мимо `getState` (`updateMetadata` — это `CopyObjectCommand`, на версионированном бакете пишет новую версию). В файле: разбор каждой находки, оценка критичности и семь текстов ответов Copilot'у (English) | Потери данных, утечек и повреждения состояния нет ни в одной из семи. Худший исход — follow-up 8: удалённый объект возвращается в All со старым содержимым, версия при этом не теряется (уже лежала в истории), лечится повторным удалением; вероятность очень низкая — нужен ответ RGW с нарушением спеки S3 плюс delete-маркер как current в разрезанной границей страницы группе | not implemented — все находки вынесены в follow-up по решению пользователя 2026-09-25; follow-up 8–11 записаны в сводный список плана раунда 1, тексты ответов публикует пользователь | [2026-09-25-pr-1331-copilot-review-round-4.md](./2026-09-25-pr-1331-copilot-review-round-4.md) |
| 2026-09-24 | Второй раунд замечаний Copilot на [PR #1331](https://github.com/cobaltcore-dev/aurora-dashboard/pull/1331) (ветка `kiryl-s3-version-pagination`, HEAD `0f992593`) — 5 замечаний: 4 принимаем, у 1 отклоняем лекарство. **Находки 1 и 2 — один баг в двух копиях:** `staleTime: 0` у `containers.getState` управляет только протуханием, а не выдачей кэша, и `isLoading` (= `isPending && isFetching`) ложен при любой закэшированной записи; кэш при этом прогрет практически всегда, потому что `useBucketInfo` держит тот же ключ со `staleTime: 30s` в шапке бакета. В `DeleteBucketModal` ущерб ограничен (непустой бакет отобьётся S3 с `BucketNotEmpty`), а в `EmptyBucketModal` протухшее `isEmpty && hasOnlyDeleteMarkers` открывает ветку, где `includeVersionsAndDeleteMarkers: true` форсится без чекбокса — полный wipe свежезалитых объектов, тот самый исходный баг PR. Фикс общий: `isLoading || isFetching`. **Находка 3 — лекарство отклонено:** проверка буфера стережёт то, что *переносится* между страницами (присваивание `pendingItems` живёт в `else` этой же проверки), пик равен предел + размер страницы = 21 000 записей и константен; безусловная проверка после слияния превратила бы дочитанный ключ на 20 001 версию в вечный `TooManyVersions` — невосстановимый отказ ровно для того профиля ключа, ради которого константа заведена. Правим док константы (его буквальное обещание недостижимо любой реализацией) + комментарий + недостающий тест на переполнение на последней странице; поведение кода не меняется. **Находка 4 — признанный откат раунда 1:** отчёт о неуспехе «Delete Versions» переезжает из тоста (`duration: Infinity`) в inline `<Message variant="error">` внутри открытой модалки. Аргумент раунда 1 («конвенция всех семи модалок — закрылась → отчиталась тостом») нигде не записан, а обратное записано прямо: B.11 `:415-416` предписывает «успех → закрыть + тост, отказ → остаться открытым + `Message`», B.5 `:245` явно разрешает operation-ошибки внутри деструктивной модалки. Оба неуспешных исхода — `variant="error"` (не `warning`: B.16 `:507` требует совпадения с `primary-danger`), поверхность повтора получается бесплатно. Удаляются два тост-хелпера, тип `PartialVersionDeleteOutcome` и пропсы `onError`/`onPartial`. **Находка 5:** три самодельных error-баннера `EmptyBucketModal` → `<Message>` с явными `role="alert"`/`aria-live="assertive"` (juno их сам не ставит). Плюс шаги на changeset (существующий абзац про «warning notification that stays on screen until dismissed» становится ложным), синхронизацию `DOCS/descriptions/pr-1331-description.md`, локальный прогон CI и тексты ответов Copilot'у на все пять | High нет; 3 Medium (принятая цена: обе деструктивные модалки теперь ждут живой `getState` — секунды на большой истории — вместо мгновенного рендера; `toast.warning` нельзя просто переложить в `Message variant="warning"` — будет третий раунд; changeset сейчас утверждает то, что шаг 4 делает ложным), несколько Low (моки `useQuery` в обеих модалках не отдают `isFetching`; тест шага 3 строит 21 страницу × 1000 записей; смена пропсов `DeleteVersionsModal` безопасна — реэкспорт из бочки `Buckets/index.tsx:43` никем не импортируется, в `client/index.ts` компонента нет). Найдено при планировании, Copilot не называл: модалка теперь остаётся открытой во время мутации и её можно закрыть, молча потеряв единственный отчёт — нужны `disableCancelButton`/`disableCloseButton` (Open Question 2) | implemented 2026-09-24 — все 6 шагов плюс два прохода triple-review (22 файла в репо + `DOCS/descriptions/pr-1331-description.md`, не закоммичено; 7/7 CI-джоб зелёные (aurora 5728 тестов, signal-openstack 177, policy-engine 320 + 1 skipped), `.po` без диффа; регрессия подтверждена по каждому фиксу, включая инверсную проверку шага 3 (с применённым лекарством Copilot новый тест краснеет, существующий `objectRouter.test.ts:1538` — нет). Два отклонения по ходу, оба пойманы гейтами: `check-i18n` дал дифф из-за сменившегося msgid (параметр `detail` → `errorMessage`), `lint` — `lingui/no-expression-in-message` (вынос в компонент с плоскими пропсами). **Security-проход добавил одну правку сверх плана (Low):** пункт 4.6 был неполон — `disableCancelButton`/`disableCloseButton` гасят только отрисованные контролы, а Esc-обработчик juno `Modal` привязан к `closeable && closeOnEsc` и этих пропсов не смотрит, то есть одним Esc отчёт о прогоне всё ещё выбрасывался; добавлен `closeOnEsc={!isPending}` + два теста (проверено по исходнику на теге `@cloudoperators/juno-ui-components@9.4.0`, не по `juno/main`). Critical/High нет; утечки сырого `error.message` не прибавилось — тот же текст, что рендерил удалённый тост. Осталось за пользователем: ручная проверка в живом UI, синхронизация тела PR на GitHub с обновлённым `descriptions/pr-1331-description.md`, публикация пяти ответов Copilot'у и коммит | [2026-09-24-pr-1331-copilot-review-round-2.md](./2026-09-24-pr-1331-copilot-review-round-2.md) |
| 2026-09-24 | Замечания Copilot-ревью на [PR #1331](https://github.com/cobaltcore-dev/aurora-dashboard/pull/1331) (ветка `kiryl-s3-version-pagination`, HEAD `1602891d`) — 6 замечаний, 5 в работу + 1 мотивированный отказ. **Главное:** находки 2, 3 и 5 это один баг в трёх копиях одного цикла — условие `if (!IsTruncated || !NextKeyMarker) break` склеивает «скан дошёл до конца» и «S3 сказал, что есть ещё, но курсора не дал», и второй случай выдаётся за полный скан. Следствия: `containers.getState` утверждает `hasOnlyDeleteMarkers` по одной странице (`EmptyBucketModal` уходит в ветку, где удаление версий форсится без чекбокса, и сносит восстановимую историю под видом «тут один мусор»); `checkDeletedContent` ставит `isPartialScan: false` для всех папок; `deleteNonCurrentVersions` не помечает `isPartial` при аборте на последней странице (пользовательских последствий нет — `ctx.req.signal` привязан к обрыву соединения, ответ никто не читает — чиним ради собственного инварианта). В `objectRouter.ts:1001` этот же паттерн уже реализован верно, т.е. это незавершённый перенос. **Находка 1 отклонена:** `requestHandler` как plain-объект — штатный вход SDK (`HttpHandlerUserInput`, `NodeHttpHandler.create`), `connectionTimeout` применяется; вместо правки кода — регрессионный тест. **Находка 4 принята с заменой лекарства:** persistent `<Message>` внутри модалки ломал бы конвенцию всех семи модалок `BucketModals` (закрылась → отчиталась тостом), поэтому нередкий `toast.warning` (`duration: Infinity`, прецедент `objectDownloadStore`) плюс попутный дефект, который Copilot не заметил — частичный успех подписан «Failed to Delete Versions». **Находка 6:** три строки с «Please» в `EmptyBucketModal` против собственного правила репозитория `.github/copilot-instructions.md:559`; остальные ~70 вхождений в клиенте — pre-existing, вынесены в follow-up. Плюс шаги на changeset (дополнить существующий, новый не заводить), синхронизацию `DOCS/descriptions/pr-1331-description.md` с опубликованным телом PR, локальный прогон CI и тексты ответов Copilot'у на все шесть замечаний | High нет; 2 Medium (фикс находки 5 честен на уровне контракта, но вкладка «Deleted» фильтрует только по `hasDeletedContent` и `isPartialScan` не читает — видимого эффекта не даст, расширение фильтра вынесено в Open Question; `pnpm check-i18n` переписывает `.po` целиком и может подтянуть посторонний дрейф), несколько Low (доп. проп `onPartial`, вечный тост, ложноположительный `isPartial`) | implemented 2026-09-24 — все 8 шагов + правки по triple-review, 21 файл, не закоммичено; 7/7 CI-джоб зелёные (aurora 5725 тестов, signal-openstack 177, policy-engine 320 + 1 skipped); каждый фикс подтверждён регрессионной проверкой (с откаченной правкой новый тест краснеет); triple-review (security + performance + architecture) — 14 находок, Critical/High нет; шесть закрыты правками в этой же ветке, главная — классификация исхода в `DeleteVersionsModal` теряла `isPartial`, когда прогон нёс и пропущенные ключи, и незавершённый скан (а сервер ставит оба флага на одном и том же пропущенном ключе), то есть у самого обычного частичного прогона пропадала фраза «запустите ещё раз»; плюс точная граница остановки скана вместо отката на целую страницу, ограничение списка ошибок в тосте, стабильный id тоста, структурированный тост-хелпер вместо склейки переведённых предложений и обязательный `onPartial`. Остальное — в «Follow-ups» плана (вынос цикла пагинации в общий хелпер, маркер возобновления, сырое `error.message` из SDK, мемоизация списков в `ObjectBrowserView`, схема имени бакета). Осталось за пользователем: ручная проверка тоста в живом UI, синхронизация тела PR на GitHub с обновлённым `descriptions/pr-1331-description.md`, публикация шести ответов Copilot'у и коммит | [2026-09-24-pr-1331-copilot-review-findings.md](./2026-09-24-pr-1331-copilot-review-findings.md) |
| 2026-09-23 | Issue #1004: кейсы ручной проверки на **реальном Ceph RGW с удалённой машины** — компаньон к плану от 2026-09-22. 26 кейсов в 5 группах (состояние бакета `containers.getState`, индикаторы удалённого контента `checkDeletedContent`, новая мутация `objects.deleteNonCurrentVersions`, свежесть кэша после мутаций, регрессии), все фикстуры и проверки — **действиями в UI**, без `aws cli` (одно исключение: B4, >100 папок, руками не собирается — оставлен на клиентский тест). Окружение: перенос ветки патчем `git apply --index` на базу `dc918d47`, SSH-туннель до :4005 для DevTools (три кейса читаются только там), правила работы с боевым проектом (деструктивные кейсы — только на бакетах `t1004-*`), приём «сжать потолок» (`S3_MAX_SCAN_PAGES`/`S3_MAX_KEYS_PER_REQUEST`) для воспроизведения `isPartialScan` без бакета на 20 000 версий. В приложении — почему локальный мок для этой ветки не годится (`ListObjectVersions` не отдаёт `<DeleteMarker>`, пагинации нет, `ListObjectsV2` считает удалённый ключ живым): осмысленны лишь 6 кейсов из 26 | — (проверка, не изменение кода) | passed 2026-09-24 — все кейсы зелёные, регрессий нет; два вне-скоупных пункта ушли в `comment-3-out-of-scope.md` | [2026-09-23-issue-1004-manual-test-cases.md](./2026-09-23-issue-1004-manual-test-cases.md) |
| 2026-09-22 | Issue #1004 «fix S3 Version Pagination for Bucket State Detection» — полный фикс двух независимых проблем, план построен на построчной верификации HEAD `dc918d47` (текст самого issue устарел: цитирует несуществующий цикл `while (!hasDeleteMarkers)`, неверные номера строк, ошибочный тезис про удаление непустого бакета, вредный «консервативный fallback»). **(A) Бэкенд:** `versioning.checkDeletedContent` — неограниченный `do{}while(true)` скан на каждую из до 100 папок параллельно, страницами по 100, без abort-signal, с пустым `catch`; early-exit требует `isFolderMarkerDeleted` и потому **никогда не срабатывает для живой папки** ⇒ полный скан префикса это норма, а не край. Рекомендовано переписать N пофолдерных сканов в **один пагинируемый скан родительского префикса** с атрибуцией ключей по первому сегменту (эквивалентность множеств доказана по коду), пофолдерный `isPartialScan` вместо подмены `hasDeletedContent`, потолок страниц, `MaxKeys`→`S3_MAX_KEYS_PER_REQUEST`, проброс `ctx.req.signal`, `connectionTimeout: 5000`. Попутно закрываются `.max(100)` против нерезаного `allFolders` (BAD_REQUEST, индикаторы молча отваливались) и перезапуск всего fan-out на каждое «Load more». **(B) Фронт:** состояние бакета считается по первым 100 версиям в трёх независимых дублирующих пробниках, два из которых не смотрят `isTruncated` (защита есть только в `EmptyBucketModal` из #1249) ⇒ пропадают пункты меню «Empty Bucket»/«Delete Versions» и неполон чек-лист причин в `DeleteBucketModal`. Рекомендован перенос на сервер новой процедурой `storage.ceph.containers.getState` (схлопывает 3 пробника в 1, серверный early-exit); попутно закрываются сопутствующий баг `includeMetadata` (`bucketObjectCount` всегда 0 вопреки комментарию) и мёртвое поле `isBucketEmptyWithVersions`. Плюс тесты с нуля (их нет ни на `bucketStateHelpers`, ни на `useBucketInfo`; все серверные кейсы `checkDeletedContent` сидят на `IsTruncated: false`) | 4 High (ограничение цикла, оставляющее `folderMarkerVersionId === undefined`, **удаляет живые папки** из вкладки All; «консервативный fallback = true» из текста issue пометил бы здоровые крупные папки как «есть мусор» — не реализуется; смена формы input `checkDeletedContent` ломает внешних потребителей BFF — решается опциональностью полей; пустой `catch` глушил `AccessDenied` — смена поведения), несколько Medium (папки за высокой водой теряют индикатор; `createMockContext` не кладёт `signal` в `req` ⇒ тест на abort зелёный ложно; 3 клиентских test-файла перестанут компилироваться после миграции) | implemented 2026-09-22 · [PR #1331](https://github.com/cobaltcore-dev/aurora-dashboard/pull/1331) (draft, ветка `kiryl-s3-version-pagination`, 50 файлов, ручная проверка на реальном Ceph RGW пройдена 2026-09-24; 14/15 шагов, шаг 4 не применим при Варианте B; все 7 CI-джоб зелёные, 5669 тестов; 4 отклонения задокументированы в плане; security — Critical/High нет; единственная Medium (fail-open пункта «Delete Versions», открывавший доступ к безвозвратному полному стиранию бакета) **исправлена 2026-09-22** возвратом к fail-closed, гейты перепрогнаны зелёными; плюс по решению пользователя в ту же ветку добавлен фикс второго, независимого бага: «Delete Versions» стирал бакет целиком — новая процедура `objects.deleteNonCurrentVersions`, при ревью которой найдены и закрыты ещё два дефекта (отложенная группа определялась по локальной сортировке вместо `NextKeyMarker`; запись без `VersionId` создавала delete-маркер вместо удаления). Итог: 28 файлов, 2 changeset'а, 5683 теста, все 7 CI-джоб зелёные) | [2026-09-22-issue-1004-s3-version-pagination-bucket-state.md](./2026-09-22-issue-1004-s3-version-pagination-bucket-state.md) |
| 2026-09-14 | Issue #1081 (epic #1246, Ceph post go-live cleanup — independent of PR #1288/#1119), **scope expanded 2026-09-15 after resolving all open questions**: invalid/unavailable `$provider` OR mismatched `$storageType` on the storage **list** route now 404 in-shell (Juno `Status code=404` inside `AuroraLayout`, not the shell-less root 404) instead of silently redirecting or rendering; "no object-store service at all" still redirects to project overview. Same provider-validity 404 policy applied to the deeper `.../$containerName/objects` route too, but that route's `$storageType`-mismatch handling deliberately stays a redirect-to-canonical (has a concrete container to land on) — this asymmetry is intentional. `checkServiceAvailability` (duplicated in 2 files) is deleted and replaced by 3 lifecycle-split guards in one shared `serviceAvailability.ts` (`requireObjectStoreService` for `beforeLoad`/redirect, `validateStorageListAccess`/`validateObjectsAccess` for `loader`/notFound), backed by a new `STORAGE_TYPE_BY_PROVIDER` constant migrated into by 5 more call sites (nav, project overview, 3 Ceph bucket/object views) | 1 High (`notFound()` must be thrown from `loader` only, never `beforeLoad`/render), 1 Medium noted as a documented follow-up (guard reads `serviceIndex["object-store"]["ceph"]` while the nav reads `serviceIndex["object-store-ceph"]["ceph"]` — masked today by `CEPH_FALLBACK_ENABLED=true`, would turn into a wrong 404 once that flag is ever set to `false`), several Medium (ordering: `beforeLoad`'s no-service redirect must still win over the new notFound checks; ~13 existing test cases across both route test files flip from redirect-assertions to `isNotFound()`-assertions, several needing an added `storageType` param to fail for the right reason) | implemented 2026-09-15 on branch `kiryl-storage-type-404-1081`, uncommitted per D5 (27 files). **Note: the Task and Risks cells above describe the 2026-09-14 revision and are stale** — a second, audited revision on 2026-09-15 added D6–D13 and materially widened the change: the "documented follow-up" Medium in the Risks cell (guard reading `serviceIndex["object-store"]["ceph"]` while the nav reads `object-store-ceph`) was **fixed here, not deferred** — both ends now resolve services by *name* across all catalog types, mirroring the backend's own `ctx.openstack.service("ceph"\|"swift")`, and `CEPH_FALLBACK_ENABLED` was deleted outright rather than left at `true` (its premise had been dead since `b9e51fc3`). Also added: distinct 404 copy per failure reason via `notFound({data:{reason}})`, a generic reusable `RouteNotFound` in `components/Error/`, shared modules moved to `client/utils/`, and analytics hardening (a raw URL segment could previously be interpolated into the emitted `onTrackEvent` action name). The audit also caught three silent test breakages the first revision would have hit, incl. the objects test file's "Canonical storageType enforcement" `beforeEach`, which relied on the deleted flag to make Ceph available. Gates: typecheck/lint/format clean, 228 files · 5671 tests green, build OK; security review clean (no Critical/High), two pre-existing follow-ups noted (`getServiceIndex` prototype-pollution hardening; raw pathname/search in analytics metadata). 5 implementation-level deviations documented in the plan. Manual verification (Testing Plan 1–10) not yet run | [2026-09-14-storage-type-404-issue-1081.md](./2026-09-14-storage-type-404-issue-1081.md) |
| 2026-09-11 | Copilot round 3 on PR #1288 (`kiryl-bucket-and-folder-validations`, after commit `4677d6b5`/now `0bb43420`): 9 comment threads collapsing into 4 issues — (1) non-CONFLICT create errors leave `CreateBucketModal`/`CreateContainerModal` open with only an auto-dismissing toast, no persistent explanation (fix: `Message variant="error"` banner, per the repo's own `.github/copilot-instructions.md`); (2) Cancel/close-during-pending — **already fixed on the branch, Copilot's comments are stale**, only regression tests needed; (3) `swiftRouter.createContainer`'s follow-up metadata/ACL/quota POST throws even after the container is genuinely created (201), leaving it in an unrecoverable "exists but unconfigured" limbo — fix: return `{created, optionsApplied, optionsError}` partial-success shape instead of throwing; (4) `BucketTableView`/`ContainerTableView` row menus use a bare `Icon` trigger instead of the codebase's established accessible `PopupMenuToggle as="div"` + labeled `Button` pattern (used at all other 16 sites) | 2 High (Issue 1's toast-vs-Message choice per the repo's own binding style-guide; Issue 3 changes a published tRPC procedure's output type, needs a changeset minor bump), several Medium (6 test files reference the toast path being removed; existing `createContainer` tests assert the old boolean return) | implemented 2026-09-11, plus a same-day triple-review follow-up fixing 7 more issues (Ceph folder-create CONFLICT handling, duplicated 412 mapping, changeset severity/history, undocumented Ceph createFolder breaking change, contradictory create+fail toast pair, Swift folder modal pending-disable gap, analytics tracking blind spot on abandon-after-failure) — 4 Lows explicitly skipped; 224 files/5602 tests + typecheck/lint/format green throughout; not committed | [2026-09-11-pr-1288-copilot-round-3.md](./2026-09-11-pr-1288-copilot-round-3.md) |
| 2026-09-10 | Copilot round 2 on PR #1288 (`kiryl-bucket-and-folder-validations`, after commit `4677d6b5`): 2 new findings, both verified real. (1) `createContainer` sends the caller's metadata/ACL/quota headers on the same PUT used to detect 202-already-exists, so a duplicate create with options silently mutates the existing container before CONFLICT — fix splits it into a probe-PUT (create-only `X-Storage-Policy` header) + a follow-up POST (reusing the existing `updateContainerMetadata` POST pattern) applied only after a genuine 201. (2) `createFolder` has zero server-side existence check and relies solely on a client-side check of one unpaginated `listObjects` page (Swift truncates at its configured listing limit) — fix makes the marker PUT an atomic `If-None-Match: *` create-if-not-exists (412→CONFLICT), verified against OpenStack Swift and Ceph RGW source (not just docs); this also fixes a real pre-existing bug where `createFolder`'s PUT call passes the headers object as the request body (wrong arg order), so today's folder markers are ~90-byte JSON blobs instead of 0-byte `application/directory` objects and none of the intended headers are ever sent | 1 High (`If-None-Match` fix is a no-op unless the PUT arg-order bug is fixed in the same step), several Medium (partial-failure handling if the post-201 metadata POST fails; existing `createFolder` test assumes the old 2-arg PUT call shape and must be updated) | implemented 2026-09-10 (all 7 steps done, no deviations; 224 files·5581 tests/typecheck/lint/format/i18n all green; security review clean — no Critical/High/Medium, one optional Low hardening note on `createContainer`'s `=== 202` branch treating any unexpected status as success, pre-existing pattern, not blocking; not committed per D5) | [2026-09-10-pr-1288-copilot-round-2.md](./2026-09-10-pr-1288-copilot-round-2.md) |
| 2026-09-10 | Fix 5 verified GitHub Copilot review findings on PR #1288 (`kiryl-bucket-and-folder-validations`): TOCTOU race in Swift `createContainer` (HEAD-then-PUT → single PUT branching on Swift's idempotent 201/202 container response), `CreateBucketModal`/`CreateContainerModal` closing the modal on any non-CONFLICT error (contradicts the PR's own stated "stays open for retry" behaviour), plus 2 missing duplicate-name tests (`CreateContainerModal.test.tsx`, `CreateFolderModal.test.tsx`) | 1 High (existing `"closes modal on error"` test in `CreateBucketModal.test.tsx` will fail if not rewritten alongside the behaviour fix), several Medium (mock `put` response needs a `status` field; mock error type needs a `data.code` field for the CONFLICT branch to be reachable in `CreateContainerModal.test.tsx`) | implemented 2026-09-10 (all 7 steps done, typecheck/lint/224 files·5573 tests/format/i18n all green, security review clean — no Critical/High; one pre-existing Low noted: 202-path metadata headers still apply to the pre-existing container before CONFLICT is thrown, documented in code, no in-repo caller affected; not committed) | [2026-09-10-pr-1288-copilot-findings.md](./2026-09-10-pr-1288-copilot-findings.md) |
| 2026-09-09 | Bulk Actions for Security Groups: multi-select + bulk delete on all three surfaces (list "Delete Selected Groups", detail Rules tab "Delete Selected Rules", detail RBAC tab "Remove Selected Policies"). Fan-out lives in the BFF via three new procedures `network.securityGroup.deleteBulk` / `securityGroupRule.deleteBulk` / `rbacPolicy.deleteBulk` returning `{successful[], failed[{id,error}]}` — the Images `processBulkOperation` pattern but with a **Network-local** helper, deliberately not renaming Compute's `failed[].imageId`. UI: Zone 3 `DataGridToolbar` with select-all (checked/indeterminate, scoped to displayed rows) + `moreVert` menu, permission-gated per surface (Images style, not Swift's always-rendered bar); undeletable rows stay selectable but are split into a "cannot be deleted" section and excluded from the submitted IDs; Swift-shape toast builders carrying their own `severity`; adopts the dead `hasAnyBulkAction`/`showSelectColumn` scaffolding already in the list; no new permission key. **All six open questions resolved 2026-09-09** (see the decisions table at the top of the plan): type-to-confirm in **all three** bulk modals (`delete`/`delete`/`remove`) after verifying all three single-item dialogs already gate on a typed word — this revised the original "typing only on the list" decision, which would have made deleting 20 rules easier than deleting 1; per-item 404 counts as success in bulk only (`ec2CredentialRouter.delete` precedent) while single-item deletion still reports NOT_FOUND; server cap 100 + `chunkSize: 5` kept, plus a new **Step 9a** client `dispatchInBatches` helper because the security groups list is not paginated so select-all can exceed the cap; `canManageAccess` left as-is (follow-up); delivery = uncommitted changes in the working tree, no commit, no push. 19 steps | 3 High (the large staged diff on `kiryl-security-groups-go-live` collides with 7 of the steps — plan is written against the working tree, not `origin/main`; two SECURITY items — every ID through `validateAndEncodeResourceId` **up-front** so a malformed ID issues zero DELETEs, and `projectScopedProcedure` scoping with no per-item `project_id`), 4 Medium (cascading Neutron DELETE fan-out; the 100-cap being reachable from an unpaginated select-all; selection vs the list's `placeholderData` refetch — derive, never `setState`; rules have no name → `formatRuleLabel` extraction) | implemented 2026-09-09 | [2026-09-09-security-groups-bulk-actions.md](./2026-09-09-security-groups-bulk-actions.md) |
| 2026-09-07 | Epic #1137 (Security Groups go-live): fix #1138 (Shared filter sends `"true"` into a `z.boolean()` input → list query dies with BAD_REQUEST; filter values shown as true/false instead of the table's Yes/No; loading state early-returns over header+toolbar; search requires Enter instead of debouncing) + #1165 (kebab cell uses `justify-end` on a `flex-col` DataGridCell → menu pushed to the bottom of the row; 3 call sites) + hardening of the #1153 class left open for `ruleId`/`policyId` (unvalidated ID interpolated into the Neutron request path) | 2 High (первый живой запуск ветки `shared !== undefined` в `securityGroupRouter.list` — Neutron может не поддержать `sort_key=project_id`; удаление early-return по ошибке может уронить FORBIDDEN-сообщение) | implemented 2026-09-07..08 (#1138 + #1165 + hardening ID→path; сверх плана добавлен фильтр Stateful — фильтруется на стороне BFF, т.к. поддержка `?stateful=` Neutron'ом не подтверждена; #1165 08.09 сначала откатывали как невоспроизводимый, затем вернули: баг виден только когда строка выше одной линии — в колонке Shared появляется вторая строка `Owner: …`; тесты по решению пользователя 2026-09-08 убраны — Testing Plan не реализован, изменения покрыты только существующими тестами репозитория; НЕ закоммичено — лежит локальными изменениями в рабочем дереве на ветке `kiryl-security-groups-go-live`, 19 файлов; typecheck/lint/224 test files 5549 tests зелёные; security-проверка без Critical/High, Medium по error.message оставлен осознанно; Риск 1 «сортировка в отфильтрованной ветке list» без автотестов — нужна ручная проверка на живом OpenStack) | [2026-09-07-security-groups-go-live-fixes.md](./2026-09-07-security-groups-go-live-fixes.md) |
| 2026-08-28 | Add an in-container actions menu for the Swift objects page (Issue #1196), mirroring Ceph's `BucketHeaderActions`/`BucketHeader` pattern with only the Swift-applicable subset (Manage Access, Preview and Edit metadata, Empty, Delete) — no versioning/policy/CORS items, no permission gating (Swift has none yet) | 2 High (placeholder `ContainerSummary` would let `DeleteContainerModal` misjudge emptiness; existing route test would throw on an unmocked real `ContainerHeader`) | implemented 2026-08-28 (all steps done, 213 test files/5423 tests + typecheck/lint/check-i18n/format green, security review clean — no Critical/High findings) | [2026-08-28-swift-in-container-actions-menu.md](./2026-08-28-swift-in-container-actions-menu.md) |
| 2026-08-28 | Fix PR #1207's outstanding review feedback + CI failures (branch `kiryl-ceph-permissions`, already merged with `main`): 2 stale test assertions blocking CI, 10 CodeRabbit/Copilot review comments (3 on code byte-identical to `origin/main` — 2 taken as trivial drive-bys, 1 real data-loss bug deferred to a follow-up PR — plus 1 already fixed by the merge), 1 duplicate test file (rewritten), 2 untranslated German strings (left empty by decision) | 1 High (deferred: shared `normalizeFilter` client/server duplication for the size-predicate data-loss follow-up) | partially implemented 2026-08-28 (code/test steps done, gates green, security review clean; GitHub thread replies pending) | [2026-08-28-pr-1207-review-feedback-fixes.md](./2026-08-28-pr-1207-review-feedback-fixes.md) |
| 2026-08-26 | Fix the 11 findings from a `/triple-review` (security+performance+architecture) on the Ceph/S3 permission-gating branch `kiryl-ceph-permissions` (`4fa8b349`, `1999dc32`, `4e917e0b`): 4 must-fix Medium (loading-state flash in `CredentialPrompt`, `s3_credentials` naming-convention violation, `CorsRulesTab` layout drift, undocumented viewer-tier rule mapping) + 7 optional Low (positional-mapping fragility, missing column-collapse a11y fix, undocumented/untested deleted-row permission mapping, dead code + missing test file, test-coverage gaps, section-grouping cleanup, rule verb-order — recommended to skip) | 2 High (rename must hit all 5 files or the whole `canUser` batch silently blanks the Ceph UI; rule/mapping drift has no compile-time check) | implemented 2026-08-26 (Steps 1-10, Step 11 skipped by decision) | [2026-08-26-ceph-permissions-review-fixes.md](./2026-08-26-ceph-permissions-review-fixes.md) |
| 2026-08-21 | Epic #608 §15: gate Ceph/S3 **mutations** (bucket/object/version/policy/CORS/lifecycle writes + EC2 credential creation) behind `storage.canUser`, client-side only, adding 11 new `storage:*` permission keys — reads/lists/views stay ungated throughout, matching the rest of the app; no `fallbackRule` mechanism (rejected as unprecedented during step-by-step review, turned out unnecessary) — revised 2026-08-24 | 1 High (credentials-gated-to-admin lockout risk) | implemented 2026-08-24 (typecheck/lint/234 test files/i18n/format all green, security review clean) | [2026-08-21-ceph-s3-permissions-gating.md](./2026-08-21-ceph-s3-permissions-gating.md) |
| 2026-08-20 | Fix the 10 findings from the `code-review` skill's 2026-08-20 pass on PR #1178 (5 correctness bugs incl. Days-field validation, Abort+tag-filter server gap, malformed-rule GET failure, order-sensitive freshness check; 3 duplication removals — rate limiter, delete modals, `normalizeFilter`; 1 dead-code/test-parity fix; 1 memoization gap) — post-dates the already-implemented 15-fix round below | 1 High (GET degradation + full-replace `set` data-loss risk), several Medium (rate-limiter/normalizeFilter shared blast radius) | partially implemented 2026-08-20 (Steps 1-5 done; 6-11 not started) | [2026-08-20-pr-1178-code-review-fixes.md](./2026-08-20-pr-1178-code-review-fixes.md) |
| 2026-08-18 | Implementation plan for the 15 confirmed PR #1178 review findings (13 ordered steps: doc fixes, server Zod refinements, mapper typing, rate-limiter cleanup across 3 routers, client validation mirrors, modal guards, form preservation, a11y labels, index derivation, i18n regen) — PR code lives only on branch `pr-1178-review`, not on `kirylDev`/`main` | 1 High (PR branch checkout prerequisite), several Medium (typecheck coupling in #12, unused-var in #5a, i18n message-id changes) | implemented 2026-08-18 | [2026-08-18-pr-1178-review-findings-fixes-implementation.md](./2026-08-18-pr-1178-review-findings-fixes-implementation.md) |
| 2026-08-18 | Fix PR #1178 review findings (CodeRabbit + Copilot, Ceph S3 lifecycle configuration) — 16 findings triaged: 15 confirmed (incl. 1 real bug reachable via UI, Finding #11), 1 rejected | 1 Major (real bug, #11), several Minor/Moderate | not implemented | [2026-08-18-pr-1178-review-findings-fixes.md](./2026-08-18-pr-1178-review-findings-fixes.md) |
| 2026-08-13 | Port Ceph Lifecycle Rules from the modal-driven flow onto the CORS tab + DataGrid architecture (3rd bucket-detail tab, per-row edit/delete, bulk delete, client-side lifecycle validation helpers) — supersedes the modal UI from the 2026-07-31 CRUD plan below | 2 High (server-import-in-client-bundle risk, lenient-read/strict-write conversion) + several Medium | implemented 2026-08-14, fixes completed 2026-08-14, all gates passing | [2026-08-13-ceph-lifecycle-rules-tab-port.md](./2026-08-13-ceph-lifecycle-rules-tab-port.md) |
| 2026-08-13 | Ceph/S3 action-naming consistency: bucket-list/object-browser/CORS-rules row menus and modal footer buttons name their target ("Empty Bucket", "Delete Folder", "Delete Object", "Upload Object", "Delete CORS Rule", etc.) instead of a generic verb — extends the CORS naming convention across Ceph (footer buttons only, not titles; Swift excluded) | 3 High, several Medium | not implemented | [2026-08-13-ceph-action-naming-consistency.md](./2026-08-13-ceph-action-naming-consistency.md) |
| 2026-08-12 | CORS Rules UI post-merge designer fixes: naming consistency, modal button variants, remove redundant "Delete CORS Rules" bucket-header action, default DataGrid layout, spacing cleanup (PR #1092) | 4 High, several Medium | implemented 2026-08-12 (complete) | [2026-08-12-cors-rules-designer-fixes.md](./2026-08-12-cors-rules-designer-fixes.md) |
| 2026-08-11 | 4 self-contained `dev-executor` fix prompts for PR #1092 review findings carried over unfixed from the 11.08.2026 report (incorrect changeset description; 3 more findings) | n/a (fix prompts, not a risk table) | not implemented | [2026-08-11-fix-pr1092-cors-review-findings.md](./2026-08-11-fix-pr1092-cors-review-findings.md) |
| 2026-08-12 | Ceph bucket list: gate row-level "Empty Bucket" action on emptiness + align row action naming with bulk/detail views (Issues #1107, #1109) | 1 Medium | implemented 2026-09-01 (naming/#1109 had already landed via PR #1207; this pass added the #1107 emptiness gate, tests, and a clean security review) | [2026-08-12-ceph-bucket-empty-action-consistency.md](./2026-08-12-ceph-bucket-empty-action-consistency.md) |
| 2026-08-07 | Redesign Ceph bucket details page: Overview/Cors Rules tabs replacing button-driven CORS flow (builds on PR #1092) | 2 Medium | implemented 2026-08-08, fixes required (review 2026-08-10) | [2026-08-07-ceph-bucket-details-tabs-cors-implemented.md](./2026-08-07-ceph-bucket-details-tabs-cors-implemented.md) |
| 2026-08-07 | Bucket details page tabs (Overview / Cors Rules) redesign, built directly on PR #1092's branch (`kiryl-ceph-cors`) rather than a separate branch — superseded by the plan below once implemented and reviewed | 2 Medium | not implemented (superseded by [2026-08-07-ceph-bucket-details-tabs-cors-implemented.md](./2026-08-07-ceph-bucket-details-tabs-cors-implemented.md) once work started) | [2026-08-07-ceph-bucket-details-tabs-cors.md](./2026-08-07-ceph-bucket-details-tabs-cors.md) |
| 2026-08-06 | Fix punch-list for `kiryl-ceph-lifecycle-rules` branch (remote-machine build): lint errors + manual-QA findings across the 4 lifecycle UI components | n/a (fix list, not a risk table) | implemented 2026-08-07 (Round 2 complete) | [2026-08-06-ceph-lifecycle-rules-remote-branch-fixes.md](./2026-08-06-ceph-lifecycle-rules-remote-branch-fixes.md) |
| 2026-08-06 | Comparison of two Ceph Lifecycle Rules implementations (Epic #608 §13): `kiryl-lifecycle-rules-speckit` vs `kiryl-lifecycle-rules-planner` | — (analysis) | analysis — informs the 2026-07-31 CRUD plan below | [2026-08-06-lifecycle-rules-implementation-comparison.md](./2026-08-06-lifecycle-rules-implementation-comparison.md) |
| 2026-08-06 | Same comparison, Russian version | — (analysis) | analysis | [2026-08-06-lifecycle-rules-implementation-comparison-ru.md](./2026-08-06-lifecycle-rules-implementation-comparison-ru.md) |
| 2026-08-04 | PRD: Bucket Tagging for Ceph S3 buckets (`bucketTagging` capability, Issue #608 §14) | — (PRD) | not implemented | [prd-bucket-tagging.md](./prd-bucket-tagging.md) |
| 2026-08-04 | Task checklist for Bucket Tagging (companion to the PRD above) | — (task list) | not implemented | [tasks-bucket-tagging.md](./tasks-bucket-tagging.md) |
| 2026-08-03 | Fix silent partial-failure reporting in `RestoreVersionModal`/`DeleteVersionModal` (`deleteVersionsBulk`, PR #1121) | 1 High | not implemented | [2026-08-03-fix-deleteversionsbulk-partial-failure.md](./2026-08-03-fix-deleteversionsbulk-partial-failure.md) |
| 2026-07-31 | Ceph/S3 epic Section 13: lifecycle rules (`buckets.getLifecycle`/`setLifecycle`/`deleteLifecycle` + add/edit/delete UI) | 3 High | implemented 2026-08-06 | [2026-07-31-ceph-lifecycle-rules-crud.md](./2026-07-31-ceph-lifecycle-rules-crud.md) |
| 2026-07-27 | Ceph/S3 epic Section 8: bulk object delete (`objects.deleteBulk` + multi-select UI) | 3 High | **implemented 2026-08-07** (PR #1121, merged into `origin/main` — confirmed via `git log`; known bug: `RestoreVersionModal`/`DeleteVersionModal` ignore `errorCount`, tracked in the 2026-08-03 fix plan above) | [2026-07-27-ceph-bulk-object-delete.md](./2026-07-27-ceph-bulk-object-delete.md) |
| 2026-07-25 | Testing plan: Section 12 CORS Configuration (PR #1092) | 1 High | not implemented | [2026-07-25-cors-configuration-testing-plan.md](./2026-07-25-cors-configuration-testing-plan.md) |
| 2026-07-25 | CORS: 2 manual test scenarios (curl + local HTML) — companion to the testing plan above; covers real browser CORS enforcement, which that plan's M9/M14 scenarios don't exercise | — (testing guide) | n/a (reference doc) | [2026-07-25-cors-manual-testing-scenarios.md](./2026-07-25-cors-manual-testing-scenarios.md) |
| 2026-07-23 | Restore a deleted Cinder volume from its most recent snapshot | 1 High | implemented 2026-07-23 | [2026-07-23-cinder-volume-restore-from-snapshot.md](./2026-07-23-cinder-volume-restore-from-snapshot.md) |
| 2026-07-23 | Ceph bucket lifecycle policies endpoint | 1 High | implemented 2026-07-23 | [2026-07-23-ceph-lifecycle-policies-endpoint.md](./2026-07-23-ceph-lifecycle-policies-endpoint.md) |
| 2026-07-23 | Add client-side pagination to the Security Groups list view, matching the pattern already shipped for Compute Images/Flavors | 1 High (conditional on wrong slice order), 3 Medium | not implemented | [2026-07-23-security-groups-list-pagination.md](./2026-07-23-security-groups-list-pagination.md) |

> Обновлено 10.08.2026: индекс дополнен 8 файлами, которые физически лежали в папке, но не попадали в таблицу (bucket-tagging PRD/tasks, security-groups pagination, CORS manual-testing companion, lifecycle-rules comparison/fix docs); статус bulk-object-delete переведён в «implemented» — PR #1121 подтверждённо смержен в `origin/main`.
