# transfer

Незакоммиченные изменения aurora-dashboard, которые едут между машинами через этот репозиторий.
Каждый файл — `git diff --cached --binary`, снятый с рабочего дерева; имя содержит базовый коммит.

## Как применить

```bash
cd <путь к aurora-dashboard>
git fetch origin
git checkout <база из имени патча>          # напр. 9775e4bb
git apply <путь к DOCS>/transfer/<файл>.patch
```

Полезное:

- `git apply --check <файл>.patch` — проверить, ляжет ли, ничего не меняя.
- `git apply --index <файл>.patch` — применить сразу в индекс (как было на исходной машине).
- `git apply --3way <файл>.patch` — если база разъехалась: смержит вместо отказа.

## Патчи

| Патч | База | Что внутри |
| --- | --- | --- |
| `ceph-bucket-policy-tenant-principals-on-6ce6aeac.patch` | `6ce6aeac` | Ветка `kiryl-ceph-bucket-policy-tenant-principals`, по фидбэку пользователей: редактор политик бакета Ceph отклонял и прятал политики с multi-tenancy — валидация Principal требовала 12-значный AWS account ID, которого в RGW не бывает, и та же проверка стояла на чтении, из-за чего рабочая политика не отображалась вовсе. Снята валидация синтаксиса Principal (теперь её делает сам RGW), чтение больше не валидирует и всегда отдаёт исходный текст, Resource ARN принимает форму с тенантом. 5 файлов (4 исходника + changeset). |
